# Phase 6: Reliability and Input Safety - Discussion Log

**Date:** 2026-04-22
**Phase:** 06-reliability-and-input-safety
**Mode:** Batched discussion

## Summary

The user requested that all Phase 6 questions be asked at once. All recommended options were accepted. One additional clarification was added: webhook delivery should retry at three different later times using separate intervals.

## Questions and Responses

### 1. Spoofed file-type policy
- Options presented:
- `A` Trust the uploaded MIME type if it matches the extension
- `B` Verify by magic bytes/signature and reject mismatches by default `(recommended)`
- `C` Verify by signature, but allow mismatches with a warning/event if the actual type is still supported
- Selected: `B`

### 2. Corrupt or unreadable file handling
- Options presented:
- `A` Fail fast as terminal input error at the first stage that proves corruption `(recommended)`
- `B` Retry once before failing, in case parsing was flaky
- `C` Attempt alternate parsers/OCR paths even when corruption is suspected
- Selected: `A`

### 3. Encrypted PDF policy
- Options presented:
- `A` Reject encrypted PDFs as unsupported/unsafe input `(recommended)`
- `B` Accept them but mark failed with a dedicated `encrypted_pdf` reason
- `C` Store them for later manual/operator replay flow
- Selected: `A`

### 4. Invalid image encoding policy
- Options presented:
- `A` Fail terminal if decode/validation fails; do not retry `(recommended)`
- `B` Retry once, then fail terminal
- `C` Attempt auto-repair/normalization before deciding
- Selected: `A`

### 5. Stage retry scope for extraction/classification service calls
- Options presented:
- `A` No automatic retries beyond webhook delivery
- `B` Retry only clearly transient failures like timeout, 5xx, connection reset `(recommended)`
- `C` Retry all failures up to a max-attempt budget
- Selected: `B`

### 6. Retry schedule shape
- Options presented:
- `A` Fixed short delay between retries
- `B` Exponential backoff with capped attempts `(recommended)`
- `C` Immediate retry once, then fixed delay
- Selected: `B`

### 7. Poison job / dead-letter outcome after max retries
- Options presented:
- `A` Mark job `failed` and leave it in the main jobs table only
- `B` Mark job `failed` and record a dedicated dead-letter/poison state plus reason `(recommended)`
- `C` Move it to a separate quarantine queue/table and remove it from normal job views
- Selected: `B`

### 8. Side-effect safety on retry/restart
- Options presented:
- `A` Permit re-running a stage if artifacts can just be overwritten
- `B` Require stage writes to be idempotent and only emit final side effects after durable persistence `(recommended)`
- `C` Require manual operator replay after any worker crash during a terminal stage
- Selected: `B`

### 9. Failure visibility in client/operator surfaces
- Options presented:
- `A` Keep current generic failure fields only
- `B` Add stable failure codes by category (`unsafe_input`, `transient_upstream`, `poison_job`, etc.) in status/events `(recommended)`
- `C` Add verbose internal diagnostics directly to the public client API
- Selected: `B`

### 10. Webhook failure interaction with overall job status
- Options presented:
- `A` A completed classification job stays `completed` even if webhook delivery later dead-letters `(recommended)`
- `B` Webhook terminal failure should turn the whole job into `failed`
- `C` Add a separate terminal job status just for delivery failure
- Selected: `A`

### 11. Unsafe-input artifact retention
- Options presented:
- `A` Keep the original uploaded artifact for traceability, but do not create derived artifacts `(recommended)`
- `B` Delete unsafe/corrupt uploads immediately
- `C` Keep both the original file and partial derived artifacts for debugging
- Selected: `A`

### 12. Operator recovery model for poison jobs
- Options presented:
- `A` Operator can inspect only; replay is a later phase `(recommended)`
- `B` Operator can replay dead-letter jobs immediately in Phase 6
- `C` No operator handling yet; logs only
- Selected: `A`

## Additional User Clarification

- Webhook delivery should retry at three different later times using separate intervals.

## Resulting Direction

- Conservative input validation
- Fail-fast terminal handling for proven unsafe/corrupt inputs
- Transient-only automatic retries with exponential backoff
- Dedicated poison/dead-letter visibility after bounded retry exhaustion
- Idempotent durable stage writes before final side effects
- Stable failure categories in status/events/operator views
- Webhook delivery failure treated separately from core job completion

---
*Generated by discuss-phase on 2026-04-22*
