---
phase: 04-classification-and-results
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - services/api/src/api_service/schemas.py
  - services/api/src/api_service/services/results.py
  - services/api/src/api_service/services/status.py
  - services/api/src/api_service/main.py
  - services/api/src/api_service/repositories/jobs.py
  - services/api/tests/test_results_api.py
  - services/api/tests/test_status_api.py
  - README.md
  - docs/foundation/local-development.md
  - docs/foundation/results-contract.md
autonomous: true
requirements:
  - CLS-01
  - CLS-02
  - CLS-03
  - CLS-04
  - DLV-03
must_haves:
  truths:
    - "The API exposes a dedicated results endpoint that returns extracted text, classification output, confidence, candidate labels, model metadata, and artifact references."
    - "The status endpoint stays lifecycle-focused while completed results are retrieved from a separate final-results surface."
    - "Results are only returned after extracted-text and classification-result artifacts are durably persisted."
  artifacts:
    - path: "services/api/src/api_service/services/results.py"
      provides: "Results assembly logic and completion guards"
    - path: "services/api/src/api_service/main.py"
      provides: "Dedicated Phase 4 results route"
    - path: "services/api/tests/test_results_api.py"
      provides: "End-to-end API tests for completed, incomplete, and missing-job result retrieval"
    - path: "README.md"
      provides: "Updated local setup and verification instructions for classification/results"
  key_links:
    - from: "services/api/src/api_service/services/results.py"
      to: "services/api/src/api_service/repositories/jobs.py"
      via: "Results assembly reads persisted extracted-text and classification metadata"
      pattern: "get_.*result|classification"
    - from: "services/api/src/api_service/main.py"
      to: "services/api/src/api_service/services/results.py"
      via: "API handler delegates final payload assembly to the results service"
      pattern: "get_job_results"
---

<objective>
Expose the dedicated results API and complete the end-to-end persistence-to-retrieval flow for Phase 4.

Purpose: Finish the Phase 4 client contract by assembling persisted extracted-text and classification artifacts into one final response while keeping status polling lifecycle-focused.
Output: Results response schemas, results service/repository queries, results endpoint, API tests, and updated local verification docs.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-classification-and-results/04-CONTEXT.md
@.planning/phases/04-classification-and-results/04-01-PLAN.md
@.planning/phases/04-classification-and-results/04-02-PLAN.md
@docs/foundation/status-polling-contract.md
@docs/foundation/results-contract.md
@services/api/src/api_service/main.py
@services/api/src/api_service/schemas.py
@services/api/src/api_service/services/status.py
@services/api/src/api_service/repositories/jobs.py
@README.md
@docs/foundation/local-development.md

<interfaces>
From `services/api/src/api_service/schemas.py`:

```python
class JobStatusResponse(BaseModel):
    job_id: str
    document_id: str
    status: str
    current_stage: str
```

From `packages/contracts/src/doc_platform_contracts/classification.py`:

```python
class DocumentClassificationResult(BaseModel):
    final_label: str
    confidence: float
    candidate_labels: list[ClassificationCandidate]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add results response schemas and a dedicated final-results service</name>
  <files>services/api/src/api_service/schemas.py, services/api/src/api_service/services/results.py, services/api/src/api_service/repositories/jobs.py, services/api/tests/test_results_api.py, services/api/tests/test_status_api.py</files>
  <read_first>services/api/src/api_service/schemas.py, services/api/src/api_service/services/status.py, services/api/src/api_service/repositories/jobs.py, docs/foundation/results-contract.md, docs/foundation/status-polling-contract.md, .planning/phases/04-classification-and-results/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A completed job returns a results payload containing full extracted text, classification output, candidate labels, classifier trace metadata, and artifact references.
    - Test 2: An incomplete job does not expose partial or missing results through the dedicated results endpoint and returns a stable client error.
    - Test 3: The status endpoint remains lifecycle-focused and does not embed the full final results payload inline.
  </behavior>
  <action>Create `services/api/src/api_service/services/results.py` with a `get_job_results(...)` service function that loads the job for the authenticated client, verifies `job.status == "completed"` and `job.current_stage == "classified"`, and assembles the final payload from the persisted extracted-text and classification-result artifacts plus DB metadata. Extend `services/api/src/api_service/schemas.py` with concrete response models `ArtifactReferenceResponse`, `ClassificationCandidateResponse`, `ClassificationMetadataResponse`, and `JobResultsResponse`. `JobResultsResponse` must include the exact fields `job_id`, `document_id`, `status`, `extracted_text`, `classification`, `artifacts`, and `completed_at`. Update `services/api/src/api_service/repositories/jobs.py` with concrete lookup helpers for the latest extracted-text artifact, classification-result artifact, and classification run by job ID. Add `services/api/tests/test_results_api.py` covering a completed result, an incomplete-job rejection, and a missing-job 404. Extend `services/api/tests/test_status_api.py` to assert the status schema does not grow an inline `classification` or `extracted_text` field.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_results_api.py services/api/tests/test_status_api.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/services/results.py` contains `get_job_results`; `services/api/src/api_service/schemas.py` contains `JobResultsResponse`; `services/api/tests/test_results_api.py` asserts fields `extracted_text`, `classification`, and `artifacts`; `services/api/tests/test_status_api.py` contains assertions that the status response remains lifecycle-only.</acceptance_criteria>
  <done>The API can assemble a stable final-results payload only when the required persisted artifacts exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the results endpoint and update local verification docs</name>
  <files>services/api/src/api_service/main.py, services/api/src/api_service/services/results.py, services/api/tests/test_results_api.py, README.md, docs/foundation/local-development.md, docs/foundation/results-contract.md</files>
  <read_first>services/api/src/api_service/main.py, services/api/src/api_service/services/results.py, services/api/tests/test_results_api.py, README.md, docs/foundation/local-development.md, .planning/phases/04-classification-and-results/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `GET /v1/jobs/{job_id}/results` is registered and returns the `JobResultsResponse` shape for completed jobs.
    - Test 2: The route enforces the same API-key auth and client scoping used by the upload/status endpoints.
    - Test 3: The README and local-development doc list the exact commands and endpoints needed to verify Phase 4 locally.
  </behavior>
  <action>Update `services/api/src/api_service/main.py` to add `GET /v1/jobs/{job_id}/results` using the existing authenticated-client and DB-session dependencies, delegating response assembly to `get_job_results(...)`. Keep the existing `GET /v1/jobs/{job_id}` route lifecycle-focused and do not embed final result fields there. Extend `services/api/tests/test_results_api.py` with route-registration and auth-scoping assertions. Update `docs/foundation/results-contract.md` with the exact route path `/v1/jobs/{job_id}/results`, the rule that results are only served after `classified` stage completion, and the payload field names. Update `README.md` and `docs/foundation/local-development.md` with the exact Phase 4 verification flow: boot the stack, verify `http://localhost:8002/healthz`, run the classifier and API result tests, upload a document, poll status, then fetch `/v1/jobs/{job_id}/results` after completion. This README update is mandatory per AGENTS.md whenever local setup or verification expectations change.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_results_api.py services/api/tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/main.py` contains `@app.get("/v1/jobs/{job_id}/results")`; `docs/foundation/results-contract.md` contains `/v1/jobs/{job_id}/results` and `classified`; `README.md` contains `services/classifier/tests/test_inference_service.py` and `/v1/jobs/{job_id}/results` in the local verification instructions.</acceptance_criteria>
  <done>External clients have a dedicated final-results endpoint, and the repo docs explain how to run and verify the new Phase 4 flow locally.</done>
</task>

</tasks>

<verification>
Run the API results and status tests, then grep the docs and route definitions for the new results endpoint, lifecycle guard, and local verification commands.
</verification>

<success_criteria>
The API exposes a stable completed-results surface that returns extracted text plus classification output only after the required artifacts are durably persisted.
</success_criteria>

<output>
After completion, create `.planning/phases/04-classification-and-results/04-03-SUMMARY.md`
</output>
