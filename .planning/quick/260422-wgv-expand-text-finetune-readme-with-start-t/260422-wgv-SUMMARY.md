# Quick Task Summary (260422-wgv)

## Goal

Make `training/text_finetune/README.md` a true start-to-finish checklist for text-only fine-tuning.

## Delivered

- Expanded README with an end-to-end flow:
  - creating `raw.jsonl`
  - preparing splits
  - training via host Python or Docker (cache reuse)
  - evaluation artifacts to check
  - export step
  - explicit note on next-step integration into `services/classifier`

