---
phase: 03-hybrid-extraction-pipeline
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - services/orchestrator/src/orchestrator_service/celery_app.py
  - services/orchestrator/src/orchestrator_service/tasks.py
  - services/orchestrator/src/orchestrator_service/extractor_client.py
  - services/orchestrator/tests/test_extraction_tasks.py
  - services/api/src/api_service/repositories/jobs.py
  - services/api/tests/test_extraction_contract.py
  - README.md
  - docs/foundation/local-development.md
  - docs/foundation/hybrid-extraction-pipeline.md
autonomous: true
requirements:
  - EXT-01
  - EXT-05
  - EXT-06
must_haves:
  truths:
    - "Scanned PDFs and images flow through OCR-aware extraction with page-aware ordering."
    - "The orchestrator records the chosen extraction path and derived artifacts durably before downstream classification."
    - "Local developers know how to run and verify the new extraction pipeline end-to-end."
  artifacts:
    - path: "services/orchestrator/src/orchestrator_service/tasks.py"
      provides: "Celery tasks for preprocess-to-extract handoff and extraction persistence"
    - path: "services/orchestrator/src/orchestrator_service/extractor_client.py"
      provides: "HTTP client wrapper for the extractor service"
    - path: "docs/foundation/hybrid-extraction-pipeline.md"
      provides: "Operational documentation for the Phase 3 extraction stages"
    - path: "README.md"
      provides: "Updated local setup and verification steps"
  key_links:
    - from: "services/orchestrator/src/orchestrator_service/tasks.py"
      to: "services/orchestrator/src/orchestrator_service/extractor_client.py"
      via: "Celery extraction tasks call the extractor HTTP client"
      pattern: "run_extraction_request"
    - from: "services/orchestrator/src/orchestrator_service/tasks.py"
      to: "services/api/src/api_service/repositories/jobs.py"
      via: "Task flow records extraction run metadata and extracted-text artifact rows"
      pattern: "create_extraction_run|create_extracted_text_artifact"
---

<objective>
Wire the OCR-aware extraction workflow into the async control plane and document the new local workflow.

Purpose: Complete the Phase 3 execution path by adding queue tasks, extractor-service calls, scanned/image OCR handling, durable extraction persistence, and updated local setup guidance.
Output: Orchestrator extraction tasks, extractor client, integration tests, hybrid extraction docs, and README/local-dev updates.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hybrid-extraction-pipeline/03-CONTEXT.md
@.planning/phases/03-hybrid-extraction-pipeline/03-01-PLAN.md
@.planning/phases/03-hybrid-extraction-pipeline/03-02-PLAN.md
@.planning/phases/02-external-ingestion-contract/02-02-SUMMARY.md
@services/orchestrator/src/orchestrator_service/celery_app.py
@services/orchestrator/tests/test_celery_app.py
@services/api/src/api_service/repositories/jobs.py
@docs/foundation/local-development.md
@README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add extraction queue tasks and scanned-input OCR handoff</name>
  <files>services/orchestrator/src/orchestrator_service/celery_app.py, services/orchestrator/src/orchestrator_service/tasks.py, services/orchestrator/src/orchestrator_service/extractor_client.py, services/orchestrator/tests/test_extraction_tasks.py</files>
  <read_first>services/orchestrator/src/orchestrator_service/celery_app.py, services/orchestrator/tests/test_celery_app.py, services/api/src/api_service/services/ingestion.py, services/extractor/src/extractor_service/main.py, docs/superpowers/plans/2026-04-21-document-inference-platform-implementation-plan.md</read_first>
  <behavior>
    - Test 1: The preprocess acceptance flow enqueues an extraction task on `document.extract`.
    - Test 2: A scanned PDF or image request generates an OCR-path extraction call that preserves page ordering metadata.
    - Test 3: The extractor client surfaces 5xx and timeout failures as retriable task errors instead of silent success.
  </behavior>
  <action>Create `services/orchestrator/src/orchestrator_service/tasks.py` with concrete Celery task names `document.preprocess.accepted` and `document.extract.run` wired to `document.preprocess` and `document.extract`. The preprocess task should move jobs from `accepted` to `extracting`, prepare an extraction request, and send it to a new `ExtractorClient` in `services/orchestrator/src/orchestrator_service/extractor_client.py` using the exact route `/v1/extractions:run`. The extraction request must carry `job_id`, `document_id`, `tenant_id`, `source_media_type`, `source_filename`, `source_artifact_id`, and any page/image storage pointers required for OCR inputs. Update `services/orchestrator/src/orchestrator_service/celery_app.py` to register the new task module and add an `enqueue_extract_job(...)` helper if needed. Add `services/orchestrator/tests/test_extraction_tasks.py` covering queue names, task registration, OCR-path request shaping for scanned/image inputs, and retry behavior on extractor client failure.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/orchestrator/tests/test_celery_app.py services/orchestrator/tests/test_extraction_tasks.py -q</automated>
  </verify>
  <acceptance_criteria>`services/orchestrator/src/orchestrator_service/tasks.py` contains `document.preprocess.accepted` and `document.extract.run`; `services/orchestrator/src/orchestrator_service/extractor_client.py` contains a client method for `/v1/extractions:run`; `services/orchestrator/tests/test_extraction_tasks.py` asserts queue usage for `document.extract` and a retry path for extractor failures.</acceptance_criteria>
  <done>The async control plane can advance accepted jobs into extractor-service execution, including OCR-path requests for scanned inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist extraction results and update the local setup docs</name>
  <files>services/api/src/api_service/repositories/jobs.py, services/api/tests/test_extraction_contract.py, docs/foundation/hybrid-extraction-pipeline.md, docs/foundation/local-development.md, README.md</files>
  <read_first>services/api/src/api_service/repositories/jobs.py, services/api/tests/test_extraction_contract.py, docs/foundation/local-development.md, README.md, docs/foundation/extraction-contract.md, .planning/phases/03-hybrid-extraction-pipeline/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Persisting an extractor response writes extraction metadata plus an `artifact_type="extracted-text"` row linked to the job.
    - Test 2: OCR-path responses preserve ordered page metadata and source artifact references.
    - Test 3: The README and local-development doc list the exact commands and endpoints needed to run Phase 3 locally.
  </behavior>
  <action>Extend `services/api/src/api_service/repositories/jobs.py` with a concrete persistence helper such as `record_extraction_completion(...)` that accepts the `ExtractedTextArtifact` payload, writes the extraction run row, and creates the derived `extracted-text` artifact metadata using the shared storage namespace. Update `services/api/tests/test_extraction_contract.py` with a persistence-oriented test that asserts ordered page numbers, `extraction_path="ocr"`, and non-empty `source_artifact_ids`. Create `docs/foundation/hybrid-extraction-pipeline.md` documenting stage names `accepted` and `extracting`, the fallback rules, artifact types `page-image`, `ocr-json`, and `extracted-text`, and the extractor endpoint contract. Update `docs/foundation/local-development.md` and `README.md` with the exact local verification steps for Phase 3: boot the stack, hit `/v1/documents:upload`, inspect extractor health at `http://localhost:8001/healthz`, and run the targeted test commands for API, orchestrator, and extractor services. This README update is mandatory per AGENTS.md whenever local setup expectations change.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_extraction_contract.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/repositories/jobs.py` contains `record_extraction_completion`; `docs/foundation/hybrid-extraction-pipeline.md` contains the exact strings `accepted`, `extracting`, `page-image`, `ocr-json`, and `extracted-text`; `README.md` contains `services/orchestrator/tests/test_extraction_tasks.py` and `services/extractor/tests/test_extraction_service.py` in the local verification instructions.</acceptance_criteria>
  <done>Extraction results are durably persisted with traceable OCR metadata, and the repo docs explain how to run and verify the Phase 3 pipeline locally.</done>
</task>

</tasks>

<verification>
Run orchestrator and API extraction tests, then grep the docs and repository helpers for the new task names, stage names, artifact types, and README commands.
</verification>

<success_criteria>
The async pipeline can route scanned/image inputs through OCR-aware extraction, persist normalized extracted-text outputs with traceability metadata, and expose accurate local setup guidance for the new stage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hybrid-extraction-pipeline/03-03-SUMMARY.md`
</output>
