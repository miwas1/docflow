---
phase: 06-reliability-and-input-safety
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/alembic/versions/0006_reliability_and_dead_letter.py
  - services/api/src/api_service/db/models.py
  - services/api/src/api_service/repositories/jobs.py
  - services/api/src/api_service/repositories/webhooks.py
  - services/api/src/api_service/services/webhooks.py
  - services/api/tests/test_status_api.py
  - services/api/tests/test_webhook_dispatch_api.py
  - services/orchestrator/src/orchestrator_service/config.py
  - services/orchestrator/src/orchestrator_service/celery_app.py
  - services/orchestrator/src/orchestrator_service/tasks.py
  - services/orchestrator/tests/test_extraction_tasks.py
  - services/orchestrator/tests/test_classification_tasks.py
  - services/orchestrator/tests/test_webhook_tasks.py
  - docs/foundation/observability.md
  - docs/foundation/local-development.md
autonomous: true
requirements:
  - SEC-03
  - SEC-04
must_haves:
  truths:
    - "Only clearly transient extractor, classifier, and webhook delivery failures are retried automatically."
    - "Retry exhaustion produces durable poison/dead-letter state plus stable terminal failure reasons that operators can inspect."
    - "Webhook delivery retries use three distinct later intervals and do not change an already completed core job back to failed."
  artifacts:
    - path: "services/api/alembic/versions/0006_reliability_and_dead_letter.py"
      provides: "Durable retry/dead-letter schema additions for jobs and delivery tracking"
    - path: "services/orchestrator/src/orchestrator_service/tasks.py"
      provides: "Transient-only retry logic and three-interval webhook backoff"
    - path: "services/api/src/api_service/repositories/jobs.py"
      provides: "Helpers to mark retrying, poison, and terminal-failure state durably"
    - path: "services/orchestrator/tests/test_webhook_tasks.py"
      provides: "Tests for separated webhook retry windows and bounded delivery exhaustion"
  key_links:
    - from: "services/orchestrator/src/orchestrator_service/tasks.py"
      to: "services/api/src/api_service/repositories/jobs.py"
      via: "Task retries and poison handling persist durable state before requeueing more work"
      pattern: "retry|dead|poison"
    - from: "services/api/src/api_service/services/webhooks.py"
      to: "services/api/tests/test_webhook_dispatch_api.py"
      via: "Webhook delivery outcome records remain inspectable even when the underlying document job stays completed"
      pattern: "delivery_status|next_retry_at"
---

<objective>
Build the bounded retry and poison-job handling layer for Phase 6.

Purpose: Satisfy SEC-03 and SEC-04 plus decisions D-06 through D-12 and D-18 by separating transient failures from terminal ones, persisting retry/dead-letter state, and introducing three distinct webhook retry intervals.
Output: Retry/dead-letter schema changes, durable repository helpers, orchestrator retry classification for extraction/classification/webhooks, tests, and updated operational docs.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reliability-and-input-safety/06-CONTEXT.md
@services/api/src/api_service/db/models.py
@services/api/src/api_service/repositories/jobs.py
@services/api/src/api_service/repositories/webhooks.py
@services/api/src/api_service/services/webhooks.py
@services/orchestrator/src/orchestrator_service/config.py
@services/orchestrator/src/orchestrator_service/celery_app.py
@services/orchestrator/src/orchestrator_service/tasks.py
@services/orchestrator/tests/test_extraction_tasks.py
@services/orchestrator/tests/test_classification_tasks.py
@services/orchestrator/tests/test_webhook_tasks.py
@docs/foundation/observability.md
@docs/foundation/local-development.md

<interfaces>
From `services/orchestrator/src/orchestrator_service/config.py`:

```python
webhook_max_attempts: int = Field(default=3, alias="WEBHOOK_MAX_ATTEMPTS")
webhook_retry_delay_seconds: int = Field(default=30, alias="WEBHOOK_RETRY_DELAY_SECONDS")
```

From `services/api/src/api_service/db/models.py`:

```python
class Job(Base):
    status: Mapped[str]
    current_stage: Mapped[str]
    failure_code: Mapped[str | None]
    failure_message: Mapped[str | None]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable retry, poison, and dead-letter state to API-owned persistence</name>
  <files>services/api/alembic/versions/0006_reliability_and_dead_letter.py, services/api/src/api_service/db/models.py, services/api/src/api_service/repositories/jobs.py, services/api/src/api_service/repositories/webhooks.py, services/api/src/api_service/services/webhooks.py, services/api/tests/test_status_api.py, services/api/tests/test_webhook_dispatch_api.py</files>
  <read_first>services/api/src/api_service/db/models.py, services/api/src/api_service/repositories/jobs.py, services/api/src/api_service/repositories/webhooks.py, services/api/src/api_service/services/webhooks.py, services/api/tests/test_status_api.py, .planning/phases/06-reliability-and-input-safety/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Jobs can persist retry metadata and poison/dead-letter state without losing the existing public status contract.
    - Test 2: Delivery exhaustion remains visible in webhook-delivery records while completed document jobs remain `completed`.
    - Test 3: Stable failure codes such as `transient_upstream_timeout`, `poison_job`, and `webhook_delivery_exhausted` can be surfaced through status/events and operator views.
  </behavior>
  <action>Create Alembic migration `services/api/alembic/versions/0006_reliability_and_dead_letter.py` that extends `jobs` with durable reliability fields `retry_count` (integer default 0), `max_retry_count` (integer default 0), `dead_lettered_at` (nullable timestamp), `dead_letter_reason` (nullable string length 128), and `terminal_failure_category` (nullable string length 64). Add matching ORM fields and indexes where useful in `services/api/src/api_service/db/models.py`. In `services/api/src/api_service/repositories/jobs.py`, create concrete helpers `mark_job_retrying(...)`, `mark_job_failed(...)`, and `mark_job_dead_lettered(...)` that update `status`, `current_stage`, `retry_count`, `failure_code`, `failure_message`, `terminal_failure_category`, and `dead_letter_reason`, and also append `JobEvent` rows with event types `stage.retry_scheduled`, `stage.failed`, and `stage.dead_lettered`. In `services/api/src/api_service/repositories/webhooks.py` and `services/api/src/api_service/services/webhooks.py`, extend outcome persistence so exhausted deliveries can store `delivery_status="failed"` with `last_error_message`, `attempt_count`, and `next_retry_at=None` while leaving the owning job’s `status` unchanged if the job is already completed. Add tests in `services/api/tests/test_status_api.py` and `services/api/tests/test_webhook_dispatch_api.py` that assert dead-letter metadata is persisted, failure codes remain stable, and webhook exhaustion does not rewrite completed jobs to failed.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src pytest services/api/tests/test_status_api.py services/api/tests/test_webhook_dispatch_api.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/alembic/versions/0006_reliability_and_dead_letter.py` exists; `services/api/src/api_service/db/models.py` contains `retry_count`, `dead_lettered_at`, and `dead_letter_reason`; `services/api/src/api_service/repositories/jobs.py` contains `mark_job_dead_lettered`; `services/api/tests/test_webhook_dispatch_api.py` asserts completed jobs stay completed after webhook exhaustion.</acceptance_criteria>
  <done>The metadata model can durably represent retry progression, poison/dead-letter exhaustion, and separated webhook delivery failure without losing core job truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transient-only stage retries and three-window webhook backoff in the orchestrator</name>
  <files>services/orchestrator/src/orchestrator_service/config.py, services/orchestrator/src/orchestrator_service/celery_app.py, services/orchestrator/src/orchestrator_service/tasks.py, services/orchestrator/tests/test_extraction_tasks.py, services/orchestrator/tests/test_classification_tasks.py, services/orchestrator/tests/test_webhook_tasks.py, docs/foundation/observability.md, docs/foundation/local-development.md</files>
  <read_first>services/orchestrator/src/orchestrator_service/config.py, services/orchestrator/src/orchestrator_service/celery_app.py, services/orchestrator/src/orchestrator_service/tasks.py, services/orchestrator/tests/test_extraction_tasks.py, services/orchestrator/tests/test_classification_tasks.py, services/orchestrator/tests/test_webhook_tasks.py, .planning/phases/06-reliability-and-input-safety/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Extraction and classification tasks retry only for clearly transient client failures such as timeout, connection reset, or 5xx-style upstream errors.
    - Test 2: After the configured max attempts, extraction/classification/webhook flows stop retrying and persist poison/dead-letter state.
    - Test 3: Webhook delivery uses three distinct later intervals instead of a single repeated delay value.
  </behavior>
  <action>Extend `services/orchestrator/src/orchestrator_service/config.py` with explicit reliability settings `EXTRACT_MAX_ATTEMPTS`, `CLASSIFY_MAX_ATTEMPTS`, `EXTRACT_RETRY_BACKOFF_SECONDS_JSON`, `CLASSIFY_RETRY_BACKOFF_SECONDS_JSON`, and `WEBHOOK_RETRY_BACKOFF_SECONDS_JSON`, where the webhook default is exactly `[30, 120, 600]`. In `services/orchestrator/src/orchestrator_service/celery_app.py`, add queue helpers `enqueue_extract_job(payload: dict, attempt: int = 1, countdown: int | None = None)` and `enqueue_classify_job(payload: dict, attempt: int = 1, countdown: int | None = None)` so retries can requeue the same stage with explicit attempt tracking. In `services/orchestrator/src/orchestrator_service/tasks.py`, wrap extractor/client failures so only transient conditions matching exact substrings `timeout`, `timed out`, `connection reset`, `temporarily unavailable`, or `502/503/504` schedule retries; unsafe-input errors such as `corrupt_pdf`, `encrypted_pdf`, `invalid_image_encoding`, or `unsupported_media_type` must fail terminal immediately. Use the exact backoff arrays from config instead of one constant delay. For webhook delivery, replace `WEBHOOK_RETRY_DELAY_SECONDS` logic with the array from `WEBHOOK_RETRY_BACKOFF_SECONDS_JSON`; attempt 1 failure schedules countdown 30, attempt 2 failure schedules countdown 120, attempt 3 failure schedules countdown 600, and exhaustion after the third retry marks the delivery `failed`. Update the orchestrator tests to cover transient extraction/classification retry scheduling, terminal unsafe-input no-retry behavior, webhook three-window backoff, and bounded exhaustion. Update `docs/foundation/observability.md` and `docs/foundation/local-development.md` with the exact reliability event names, retry categories, and the local verification flow for forcing transient failures and observing backoff/dead-letter behavior.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/orchestrator/src pytest services/orchestrator/tests/test_extraction_tasks.py services/orchestrator/tests/test_classification_tasks.py services/orchestrator/tests/test_webhook_tasks.py -q</automated>
  </verify>
  <acceptance_criteria>`services/orchestrator/src/orchestrator_service/config.py` contains `WEBHOOK_RETRY_BACKOFF_SECONDS_JSON`; `services/orchestrator/src/orchestrator_service/tasks.py` contains logic for `corrupt_pdf`/`encrypted_pdf` terminal no-retry handling and webhook countdowns `30`, `120`, and `600`; orchestrator tests cover retry scheduling and terminal exhaustion.</acceptance_criteria>
  <done>The orchestrator now retries only transient stage failures, uses three distinct webhook retry windows, and dead-letters exhausted work instead of looping indefinitely.</done>
</task>

</tasks>

<verification>
Run the API and orchestrator retry/dead-letter test suites, then grep for `dead_letter_reason`, `mark_job_dead_lettered`, `WEBHOOK_RETRY_BACKOFF_SECONDS_JSON`, and the countdown values `30`, `120`, and `600`.
</verification>

<success_criteria>
Transient failures retry safely with bounded backoff, exhausted work is dead-lettered durably, and webhook delivery failures remain observable without corrupting the already-completed core job state.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-and-input-safety/06-02-SUMMARY.md`
</output>
