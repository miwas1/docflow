---
phase: 05-client-delivery-and-operator-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - services/api/src/api_service/config.py
  - services/api/src/api_service/dependencies.py
  - services/api/src/api_service/main.py
  - services/api/src/api_service/repositories/webhooks.py
  - services/api/src/api_service/services/webhooks.py
  - services/api/tests/test_webhook_dispatch_api.py
  - services/orchestrator/src/orchestrator_service/celery_app.py
  - services/orchestrator/src/orchestrator_service/config.py
  - services/orchestrator/src/orchestrator_service/tasks.py
  - services/orchestrator/src/orchestrator_service/webhook_client.py
  - services/orchestrator/tests/test_webhook_tasks.py
  - docs/foundation/webhook-contract.md
  - docs/foundation/local-development.md
autonomous: true
requirements:
  - DLV-04
must_haves:
  truths:
    - "Completed and failed jobs can trigger asynchronous webhook delivery without blocking upload, status, or results requests."
    - "Non-2xx webhook responses retry with a bounded policy and write observable delivery outcomes."
    - "Webhook dispatch always uses already-persisted state and never invents a separate source of truth for results."
  artifacts:
    - path: "services/orchestrator/src/orchestrator_service/tasks.py"
      provides: "Queued webhook delivery task with retry handling"
    - path: "services/orchestrator/src/orchestrator_service/webhook_client.py"
      provides: "HTTP dispatcher that signs and sends webhook requests"
    - path: "services/api/src/api_service/main.py"
      provides: "Internal API endpoints used by the dispatcher to load payloads and persist delivery outcomes"
    - path: "services/orchestrator/tests/test_webhook_tasks.py"
      provides: "Task-level tests for success, retry, and terminal failure paths"
  key_links:
    - from: "services/orchestrator/src/orchestrator_service/tasks.py"
      to: "services/api/src/api_service/services/webhooks.py"
      via: "Dispatcher fetches canonical persisted payloads and records outcomes through API-owned helpers"
      pattern: "webhook"
    - from: "services/orchestrator/src/orchestrator_service/celery_app.py"
      to: "document.webhook"
      via: "Queued delivery lane remains asynchronous per D-03"
      pattern: "document.webhook"
---

<objective>
Implement the asynchronous webhook dispatcher on top of the Phase 05 contract and persistence layer.

Purpose: Make signed completion and failure callbacks real without moving delivery into the synchronous client path, while preserving bounded retries and durable attempt outcomes per D-02 and D-03.
Output: Internal API delivery helpers, orchestrator dispatcher task/client, task and API tests, and local delivery verification docs.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md
@.planning/phases/05-client-delivery-and-operator-visibility/05-01-PLAN.md
@docs/foundation/webhook-contract.md
@services/api/src/api_service/services/webhooks.py
@services/api/src/api_service/repositories/webhooks.py
@services/orchestrator/src/orchestrator_service/celery_app.py
@services/orchestrator/src/orchestrator_service/tasks.py
@services/orchestrator/src/orchestrator_service/config.py

<interfaces>
From `services/orchestrator/src/orchestrator_service/celery_app.py`:

```python
QUEUE_NAMES = (
    "document.preprocess",
    "document.extract",
    "document.classify",
    "document.persist",
    "document.webhook",
)
```

From `services/api/src/api_service/services/webhooks.py` created in 05-01:

```python
def build_terminal_webhook_payload(... ) -> TerminalWebhookPayload: ...
def sign_webhook_payload(secret: str, raw_payload: bytes) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose internal webhook payload and delivery-state endpoints in the API service</name>
  <files>services/api/src/api_service/config.py, services/api/src/api_service/dependencies.py, services/api/src/api_service/main.py, services/api/src/api_service/repositories/webhooks.py, services/api/src/api_service/services/webhooks.py, services/api/tests/test_webhook_dispatch_api.py</files>
  <read_first>services/api/src/api_service/config.py, services/api/src/api_service/dependencies.py, services/api/src/api_service/main.py, services/api/src/api_service/services/webhooks.py, services/api/src/api_service/repositories/webhooks.py, .planning/phases/05-client-delivery-and-operator-visibility/05-01-PLAN.md</read_first>
  <behavior>
    - Test 1: An internal authenticated caller can request the canonical terminal webhook payload for a given `job_id` and receive the persisted payload plus destination URL and signing secret metadata.
    - Test 2: A second internal endpoint records delivery success, HTTP failures, retry scheduling, and terminal exhaustion into `webhook_deliveries`.
    - Test 3: Public API-key clients cannot call the internal delivery endpoints.
  </behavior>
  <action>Add `INTERNAL_SERVICE_TOKEN` to `services/api/src/api_service/config.py` and create an internal dependency in `services/api/src/api_service/dependencies.py` that requires `Authorization: Bearer <token>`. Extend `services/api/src/api_service/services/webhooks.py` with `get_webhook_dispatch_payload(job_id: str)` and `record_webhook_delivery_outcome(...)`. `get_webhook_dispatch_payload(...)` must load the active subscription for the job's `client_id`, build the Phase 05 terminal payload, and create or refresh the `webhook_deliveries` row before dispatch. `record_webhook_delivery_outcome(...)` must write `delivery_status` values `pending`, `retrying`, `delivered`, or `failed`, update `attempt_count`, `last_http_status`, `last_error_message`, and `next_retry_at`, and leave a durable record keyed by `job_id`. Add two internal API routes in `services/api/src/api_service/main.py`: `GET /internal/webhooks/jobs/{job_id}/dispatch` and `POST /internal/webhooks/jobs/{job_id}/deliveries/{delivery_id}`. Cover these flows in `services/api/tests/test_webhook_dispatch_api.py` using the same in-memory DB style as the other API tests.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src pytest services/api/tests/test_webhook_dispatch_api.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/main.py` contains `/internal/webhooks/jobs/{job_id}/dispatch` and `/internal/webhooks/jobs/{job_id}/deliveries/{delivery_id}`; `services/api/src/api_service/config.py` contains `INTERNAL_SERVICE_TOKEN`; `services/api/tests/test_webhook_dispatch_api.py` asserts non-internal callers are rejected and delivery outcomes are persisted.</acceptance_criteria>
  <done>The orchestrator has internal API surfaces it can safely use to fetch canonical webhook payloads and persist delivery results without taking ownership of API data models.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Queue signed webhook delivery with bounded retries in the orchestrator</name>
  <files>services/orchestrator/src/orchestrator_service/celery_app.py, services/orchestrator/src/orchestrator_service/config.py, services/orchestrator/src/orchestrator_service/tasks.py, services/orchestrator/src/orchestrator_service/webhook_client.py, services/orchestrator/tests/test_webhook_tasks.py, docs/foundation/webhook-contract.md, docs/foundation/local-development.md</files>
  <read_first>services/orchestrator/src/orchestrator_service/celery_app.py, services/orchestrator/src/orchestrator_service/config.py, services/orchestrator/src/orchestrator_service/tasks.py, services/api/src/api_service/main.py, docs/foundation/webhook-contract.md, .planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A `document.webhook.deliver` task fetches the canonical payload, sends the signed request to the subscription URL, and records a delivered status for 2xx callbacks.
    - Test 2: Non-2xx or timeout failures schedule retries up to a concrete maximum attempt count, then mark the delivery as permanently failed without looping forever per DLV-04.
    - Test 3: Delivery only happens for terminal-state events and never blocks the synchronous upload or results paths per D-03.
  </behavior>
  <action>Add `API_BASE_URL`, `INTERNAL_SERVICE_TOKEN`, `WEBHOOK_TIMEOUT_SECONDS`, `WEBHOOK_MAX_ATTEMPTS`, and `WEBHOOK_RETRY_DELAY_SECONDS` to `services/orchestrator/src/orchestrator_service/config.py`. Extend `services/orchestrator/src/orchestrator_service/celery_app.py` with `enqueue_webhook_delivery(job_id: str)` that targets `document.webhook`. Create `services/orchestrator/src/orchestrator_service/webhook_client.py` with methods `fetch_dispatch_payload(job_id: str)` and `deliver(payload: dict, target_url: str, signature: str)`. Add a Celery task named `document.webhook.deliver` in `services/orchestrator/src/orchestrator_service/tasks.py` that calls the internal API dispatch endpoint, issues the outbound HTTP POST with `Content-Type: application/json` and `X-DocPlatform-Signature`, and then posts the delivery outcome back to the internal API outcome endpoint. Implement bounded retry behavior using the exact config values `WEBHOOK_MAX_ATTEMPTS` and `WEBHOOK_RETRY_DELAY_SECONDS`; when the max is exceeded, the task must stop retrying and mark the delivery `failed`. Update `docs/foundation/webhook-contract.md` and `docs/foundation/local-development.md` with the local verification path: run a simple local callback receiver, submit a job, wait for a terminal state, and verify signed delivery plus retry behavior.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/orchestrator/src pytest services/orchestrator/tests/test_webhook_tasks.py -q</automated>
  </verify>
  <acceptance_criteria>`services/orchestrator/src/orchestrator_service/tasks.py` contains `@celery_app.task(name="document.webhook.deliver")`; `services/orchestrator/src/orchestrator_service/config.py` contains `WEBHOOK_MAX_ATTEMPTS`; `services/orchestrator/tests/test_webhook_tasks.py` covers success and bounded-retry exhaustion; `docs/foundation/local-development.md` contains `document.webhook.deliver` or a local webhook receiver verification flow.</acceptance_criteria>
  <done>Signed webhook delivery is asynchronous, retryable, bounded, and durably observable without adding callback work to the public API request path.</done>
</task>

</tasks>

<verification>
Run the API dispatch test and the orchestrator webhook task test, then grep the dispatcher task, config, and docs for the internal routes, signature header, and retry settings.
</verification>

<success_criteria>
Terminal-state webhooks are delivered from the dedicated webhook queue with a bounded retry policy and API-owned delivery records.
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-delivery-and-operator-visibility/05-02-SUMMARY.md`
</output>
