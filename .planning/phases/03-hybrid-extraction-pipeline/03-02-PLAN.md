---
phase: 03-hybrid-extraction-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - services/extractor/pyproject.toml
  - services/extractor/src/extractor_service/config.py
  - services/extractor/src/extractor_service/extraction.py
  - services/extractor/src/extractor_service/main.py
  - services/extractor/tests/test_extraction_service.py
autonomous: true
requirements:
  - EXT-01
  - EXT-02
  - EXT-03
  - EXT-04
must_haves:
  truths:
    - "The extractor can decide between direct parsing and OCR according to the locked PDF routing rules."
    - "Native PDFs, DOCX, TXT, and JSON inputs all produce the same extracted-text contract."
    - "Fallback decisions are deterministic and encoded in the returned extraction metadata."
  artifacts:
    - path: "services/extractor/src/extractor_service/extraction.py"
      provides: "Hybrid extraction routing and format-specific adapters"
    - path: "services/extractor/src/extractor_service/main.py"
      provides: "HTTP extraction surface for orchestrator calls"
    - path: "services/extractor/tests/test_extraction_service.py"
      provides: "Behavior tests for routing, direct extraction, and fallback"
  key_links:
    - from: "services/extractor/src/extractor_service/main.py"
      to: "services/extractor/src/extractor_service/extraction.py"
      via: "HTTP handler delegates to the shared extraction runner"
      pattern: "run_extraction"
    - from: "services/extractor/src/extractor_service/extraction.py"
      to: "packages/contracts/src/doc_platform_contracts/extraction.py"
      via: "Returned payloads match the shared extracted-text contract"
      pattern: "ExtractedTextArtifact"
---

<objective>
Implement the extractor service's hybrid routing and direct-parsing adapters for digital formats.

Purpose: Turn the empty extractor boundary into a real Phase 3 service that can normalize PDFs, DOCX, TXT, and JSON into one extracted-text payload before orchestration wiring is added.
Output: Extractor service extraction module, HTTP endpoint, config updates, and tests for routing/fallback behavior.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-hybrid-extraction-pipeline/03-CONTEXT.md
@.planning/phases/03-hybrid-extraction-pipeline/03-01-PLAN.md
@services/extractor/src/extractor_service/main.py
@services/extractor/src/extractor_service/config.py
@services/extractor/pyproject.toml
@services/extractor/tests/test_health.py
@packages/contracts/src/doc_platform_contracts/extraction.py

<interfaces>
From `services/extractor/src/extractor_service/main.py`:

```python
def create_app() -> FastAPI:
    app = FastAPI(title="Document Platform Extractor", version="0.1.0")
```

From `services/extractor/src/extractor_service/config.py`:

```python
class ExtractorSettings(BasePlatformSettings):
    host: str
    port: int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build deterministic hybrid routing and digital-format adapters</name>
  <files>services/extractor/src/extractor_service/extraction.py, services/extractor/src/extractor_service/config.py, services/extractor/pyproject.toml, services/extractor/tests/test_extraction_service.py</files>
  <read_first>services/extractor/src/extractor_service/config.py, services/extractor/pyproject.toml, services/extractor/tests/test_health.py, packages/contracts/src/doc_platform_contracts/extraction.py, .planning/phases/03-hybrid-extraction-pipeline/03-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A PDF with usable embedded text returns `extraction_path="direct"` and `fallback_used=False`.
    - Test 2: A PDF with empty or unusable embedded text falls back to `extraction_path="ocr"` with `fallback_used=True` and `fallback_reason="embedded_text_unusable"`.
    - Test 3: DOCX, TXT, and JSON inputs all return the shared extracted-text payload shape with ordered `pages`.
  </behavior>
  <action>Create `services/extractor/src/extractor_service/extraction.py` with a `run_extraction(...)` entry point and format-specific adapter functions for PDF, DOCX, TXT, and JSON. Implement the locked PDF rule per D-01 and D-02: inspect embedded PDF text first, treat empty/whitespace-only or below-threshold extraction as unusable, and switch to the OCR path with `fallback_reason="embedded_text_unusable"`. For DOCX, TXT, and JSON, use direct parsing adapters that return the normalized `ExtractedTextArtifact` payload from `packages/contracts`. Update `services/extractor/pyproject.toml` and `services/extractor/src/extractor_service/config.py` with any exact config keys needed for parser thresholds, including `PDF_TEXT_MIN_CHARS` or equivalent env alias, but do not add classification-related settings. Add `services/extractor/tests/test_extraction_service.py` with concrete direct-routing and fallback tests using representative in-memory inputs.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/extractor/tests/test_extraction_service.py -q</automated>
  </verify>
  <acceptance_criteria>`services/extractor/src/extractor_service/extraction.py` contains `run_extraction`, `extract_pdf`, `extract_docx`, `extract_text`, and `extract_json`; the file contains the exact strings `embedded_text_unusable`, `direct`, and `ocr`; `services/extractor/tests/test_extraction_service.py` contains test cases for PDF, DOCX, TXT, and JSON inputs.</acceptance_criteria>
  <done>The extractor service can normalize supported digital formats and expose deterministic fallback metadata before OCR fan-out is wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the extractor HTTP surface for orchestrated extraction requests</name>
  <files>services/extractor/src/extractor_service/main.py, services/extractor/src/extractor_service/extraction.py, services/extractor/tests/test_extraction_service.py</files>
  <read_first>services/extractor/src/extractor_service/main.py, services/extractor/src/extractor_service/extraction.py, services/extractor/tests/test_extraction_service.py, packages/contracts/src/doc_platform_contracts/extraction.py</read_first>
  <behavior>
    - Test 1: `POST /v1/extractions:run` accepts a request with `job_id`, `document_id`, `media_type`, and source artifact reference, then returns the normalized extraction payload.
    - Test 2: The HTTP response includes `extraction_path`, `page_count`, and `source_artifact_ids`.
    - Test 3: Unsupported request media types receive a stable 4xx error instead of a silent failure.
  </behavior>
  <action>Update `services/extractor/src/extractor_service/main.py` to add a JSON `POST /v1/extractions:run` endpoint that calls `run_extraction(...)` from `extraction.py`. The request contract must include the exact fields `job_id`, `document_id`, `tenant_id`, `source_media_type`, `source_filename`, `source_artifact_id`, and either inline content or a dereferenceable storage pointer field chosen consistently for the service. Return the `ExtractedTextArtifact` payload directly. Reuse the existing FastAPI error pattern used in the API service where practical, but keep the extractor independent. Extend `services/extractor/tests/test_extraction_service.py` with API-level tests that hit the endpoint and assert the returned `extraction_path`, `page_count`, and `source_artifact_ids` fields.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/extractor/tests/test_extraction_service.py services/extractor/tests/test_health.py -q</automated>
  </verify>
  <acceptance_criteria>`services/extractor/src/extractor_service/main.py` contains `@app.post("/v1/extractions:run")`; the handler returns a payload containing `extraction_path`, `page_count`, and `source_artifact_ids`; test assertions cover both a successful `direct` response and an unsupported-media failure.</acceptance_criteria>
  <done>The extractor service exposes a concrete Phase 3 extraction API that the orchestrator can call without assuming local module access.</done>
</task>

</tasks>

<verification>
Run the extractor tests and grep the new route and routing constants to confirm the service now supports direct extraction plus deterministic fallback.
</verification>

<success_criteria>
The extractor service can receive extraction requests, choose direct parsing or OCR per the locked rules, and return the normalized extracted-text payload for supported digital formats.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hybrid-extraction-pipeline/03-02-SUMMARY.md`
</output>
