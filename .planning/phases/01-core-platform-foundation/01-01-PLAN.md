---
phase: 01-core-platform-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - .gitignore
  - Makefile
  - .env.example
  - packages/contracts/pyproject.toml
  - packages/contracts/src/doc_platform_contracts/__init__.py
  - packages/contracts/src/doc_platform_contracts/settings.py
  - services/api/pyproject.toml
  - services/api/src/api_service/main.py
  - services/api/src/api_service/config.py
  - services/api/tests/test_health.py
  - services/orchestrator/pyproject.toml
  - services/orchestrator/src/orchestrator_service/celery_app.py
  - services/orchestrator/src/orchestrator_service/config.py
  - services/orchestrator/tests/test_celery_app.py
  - services/extractor/pyproject.toml
  - services/extractor/src/extractor_service/main.py
  - services/extractor/src/extractor_service/config.py
  - services/extractor/tests/test_health.py
  - services/classifier/pyproject.toml
  - services/classifier/src/classifier_service/main.py
  - services/classifier/src/classifier_service/config.py
  - services/classifier/tests/test_health.py
autonomous: true
requirements: [OPS-01, SEC-06]
user_setup: []
must_haves:
  truths:
    - "The repository defines separate service workspaces for api, orchestrator, extractor, and classifier instead of a single application package."
    - "Each HTTP-facing service exposes a health endpoint and loads configuration from environment-based settings modules."
    - "The worker/control-plane service has a real Celery application configured to use RabbitMQ as its broker URL source."
  artifacts:
    - "packages/contracts/src/doc_platform_contracts/settings.py"
    - "services/api/src/api_service/main.py"
    - "services/orchestrator/src/orchestrator_service/celery_app.py"
    - "services/extractor/src/extractor_service/main.py"
    - "services/classifier/src/classifier_service/main.py"
  key_links:
    - "Service config modules import shared settings helpers from packages/contracts."
    - "Health tests import each service entrypoint and verify the expected health contract."
---

<objective>
Create the separate service workspace and baseline Python application scaffolds for the platform foundation.

Purpose: Phase 1 needs real service boundaries before persistence, messaging, and deployment work can be layered on top.
Output: Independent service directories, shared contracts package, environment/config conventions, and runnable health-entry scaffolds.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-platform-foundation/01-CONTEXT.md
@system_design.txt
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the separate service workspace skeleton</name>
  <files>README.md, .gitignore, Makefile, .env.example, packages/contracts/pyproject.toml, packages/contracts/src/doc_platform_contracts/__init__.py, packages/contracts/src/doc_platform_contracts/settings.py, services/api/pyproject.toml, services/orchestrator/pyproject.toml, services/extractor/pyproject.toml, services/classifier/pyproject.toml</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-core-platform-foundation/01-CONTEXT.md, system_design.txt, AGENTS.md, README.md, .gitignore</read_first>
  <action>Create a root workspace that preserves separate-service boundaries while staying manageable in the current project repository. Add a top-level README that explains the four service workspaces (`services/api`, `services/orchestrator`, `services/extractor`, `services/classifier`), the shared contracts package (`packages/contracts`), and the future infrastructure directory (`infra/terraform`). Add `.gitignore` entries for Python caches, virtual environments, local `.env`, pytest caches, coverage files, and Terraform state. Add a `Makefile` with concrete targets `bootstrap`, `test`, `test-api`, `test-orchestrator`, `test-extractor`, and `test-classifier`. Add `.env.example` with exact keys `POSTGRES_DSN=postgresql+psycopg://doc_platform:doc_platform@localhost:5432/doc_platform`, `RABBITMQ_URL=amqp://doc_platform:doc_platform@localhost:5672/doc_platform`, `OBJECT_STORAGE_ENDPOINT=http://localhost:9000`, `OBJECT_STORAGE_BUCKET=doc-platform-artifacts`, `OBJECT_STORAGE_ACCESS_KEY=minioadmin`, `OBJECT_STORAGE_SECRET_KEY=minioadmin`, `API_HOST=0.0.0.0`, `API_PORT=8000`, `EXTRACTOR_HOST=0.0.0.0`, `EXTRACTOR_PORT=8001`, `CLASSIFIER_HOST=0.0.0.0`, and `LOG_LEVEL=INFO`. Use Python 3.12 and `pyproject.toml` in every service/package. In `packages/contracts/src/doc_platform_contracts/settings.py`, define a shared Pydantic settings base with typed fields for the exact environment variables above so all services can import the same configuration contract.</action>
  <verify>rg -n "services/api|services/orchestrator|services/extractor|services/classifier|packages/contracts" README.md && rg -n "POSTGRES_DSN|RABBITMQ_URL|OBJECT_STORAGE_BUCKET" .env.example && rg -n "bootstrap|test-api|test-orchestrator|test-extractor|test-classifier" Makefile</verify>
  <acceptance_criteria>
    - `README.md` contains `services/api`, `services/orchestrator`, `services/extractor`, and `services/classifier`
    - `.env.example` contains `POSTGRES_DSN=postgresql+psycopg://doc_platform:doc_platform@localhost:5432/doc_platform`
    - `.env.example` contains `RABBITMQ_URL=amqp://doc_platform:doc_platform@localhost:5672/doc_platform`
    - `Makefile` contains `test-api`, `test-orchestrator`, `test-extractor`, and `test-classifier`
    - `packages/contracts/src/doc_platform_contracts/settings.py` contains `class BasePlatformSettings`
  </acceptance_criteria>
  <done>The repo exposes a clear separate-service workspace with shared environment/config contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold service entrypoints and health contracts</name>
  <files>services/api/src/api_service/main.py, services/api/src/api_service/config.py, services/api/tests/test_health.py, services/orchestrator/src/orchestrator_service/celery_app.py, services/orchestrator/src/orchestrator_service/config.py, services/orchestrator/tests/test_celery_app.py, services/extractor/src/extractor_service/main.py, services/extractor/src/extractor_service/config.py, services/extractor/tests/test_health.py, services/classifier/src/classifier_service/main.py, services/classifier/src/classifier_service/config.py, services/classifier/tests/test_health.py</files>
  <read_first>.planning/phases/01-core-platform-foundation/01-CONTEXT.md, system_design.txt, packages/contracts/src/doc_platform_contracts/settings.py, services/api/pyproject.toml, services/orchestrator/pyproject.toml, services/extractor/pyproject.toml, services/classifier/pyproject.toml</read_first>
  <action>Implement minimal but real service entrypoints. For `services/api`, `services/extractor`, and `services/classifier`, create FastAPI applications with `GET /healthz` returning JSON that includes `status: "ok"` and `service` set respectively to `api`, `extractor`, or `classifier`. In each service `config.py`, subclass the shared settings base and set the exact default ports `8000`, `8001`, and `8002` respectively. In `services/orchestrator/src/orchestrator_service/celery_app.py`, create a Celery app named `doc_platform_orchestrator` configured from `RABBITMQ_URL` and define queues named `document.preprocess`, `document.extract`, `document.classify`, `document.persist`, and `document.webhook`. Add simple tests that import the entrypoints directly: HTTP service tests should assert `/healthz` returns the exact `service` value, and the Celery test should assert the app main name and queue names exist. Do not add business workflow tasks yet; only create the runnable control-plane scaffold and the service boundaries that later phases will extend.</action>
  <verify>pytest services/api/tests/test_health.py services/orchestrator/tests/test_celery_app.py services/extractor/tests/test_health.py services/classifier/tests/test_health.py -q</verify>
  <acceptance_criteria>
    - `services/api/src/api_service/main.py` contains `@app.get("/healthz")`
    - `services/extractor/src/extractor_service/main.py` contains `"service": "extractor"`
    - `services/classifier/src/classifier_service/main.py` contains `"service": "classifier"`
    - `services/orchestrator/src/orchestrator_service/celery_app.py` contains `doc_platform_orchestrator`
    - `services/orchestrator/src/orchestrator_service/celery_app.py` contains `document.preprocess`, `document.extract`, `document.classify`, `document.persist`, and `document.webhook`
  </acceptance_criteria>
  <done>Each service boundary is represented by real code and smoke tests instead of placeholder folders only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest services/api/tests/test_health.py services/orchestrator/tests/test_celery_app.py services/extractor/tests/test_health.py services/classifier/tests/test_health.py -q`
- [ ] `rg -n "GET /healthz|/healthz" services/api services/extractor services/classifier`
- [ ] `rg -n "document.preprocess|document.extract|document.classify|document.persist|document.webhook" services/orchestrator/src/orchestrator_service/celery_app.py`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository exposes separate runnable service scaffolds that match the Phase 1 context decisions
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-platform-foundation/01-01-SUMMARY.md`
</output>
