---
phase: 05-client-delivery-and-operator-visibility
plan: 04
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - services/api/src/api_service/config.py
  - services/api/src/api_service/dependencies.py
  - services/api/src/api_service/schemas.py
  - services/api/src/api_service/repositories/operator_dashboard.py
  - services/api/src/api_service/services/operator_dashboard.py
  - services/api/src/api_service/templates/operator_dashboard.html
  - services/api/src/api_service/main.py
  - services/api/tests/test_operator_dashboard.py
  - docs/foundation/operator-dashboard.md
  - docs/foundation/local-development.md
  - README.md
autonomous: true
requirements:
  - OPS-03
  - OPS-04
must_haves:
  truths:
    - "Operators can view queued, running, completed, and failed job counts plus searchable job history from an internal protected surface."
    - "Operators can open a job and inspect stage progression, failure diagnostics, model versions, and webhook delivery history without manual database queries."
    - "The dashboard surface is internal-only and separate from the external client API contract."
  artifacts:
    - path: "services/api/src/api_service/services/operator_dashboard.py"
      provides: "Operator dashboard summary, list, and detail aggregation logic"
    - path: "services/api/src/api_service/templates/operator_dashboard.html"
      provides: "Protected internal dashboard UI"
    - path: "services/api/tests/test_operator_dashboard.py"
      provides: "Tests for operator auth, counts, filtering, drill-downs, and webhook visibility"
    - path: "README.md"
      provides: "Updated local setup instructions for starting and verifying the operator dashboard"
  key_links:
    - from: "services/api/src/api_service/services/operator_dashboard.py"
      to: "services/api/src/api_service/repositories/operator_dashboard.py"
      via: "Dashboard aggregates jobs, job events, extraction runs, classification runs, and webhook deliveries"
      pattern: "webhook|classification|extraction"
    - from: "services/api/src/api_service/main.py"
      to: "services/api/src/api_service/templates/operator_dashboard.html"
      via: "Protected internal route renders the operator dashboard and companion JSON drill-down APIs"
      pattern: "operator"
---

<objective>
Ship the internal operator dashboard and drill-down APIs after delivery and observability signals exist.

Purpose: Satisfy OPS-03 and OPS-04 by giving internal operations staff one protected place to inspect queue health, stage progression, failure reasons, model versions, and webhook delivery outcomes per D-07 through D-12.
Output: Operator auth/config, dashboard aggregation repository and service, HTML dashboard template, internal JSON endpoints, tests, and updated local setup docs.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md
@.planning/phases/05-client-delivery-and-operator-visibility/05-01-PLAN.md
@.planning/phases/05-client-delivery-and-operator-visibility/05-02-PLAN.md
@.planning/phases/05-client-delivery-and-operator-visibility/05-03-PLAN.md
@services/api/src/api_service/db/models.py
@services/api/src/api_service/schemas.py
@services/api/src/api_service/main.py
@services/api/src/api_service/repositories/webhooks.py
@docs/foundation/status-polling-contract.md
@docs/foundation/results-contract.md
@docs/foundation/observability.md

<interfaces>
From `services/api/src/api_service/db/models.py`:

```python
class JobEvent(Base):
    event_type: Mapped[str]
    stage: Mapped[str]
    payload_json: Mapped[dict]

class ExtractionRun(Base):
    extraction_path: Mapped[str]
    trace_json: Mapped[dict]

class ClassificationRun(Base):
    final_label: Mapped[str]
    confidence: Mapped[float]
    trace_json: Mapped[dict]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add protected operator auth and dashboard data APIs</name>
  <files>services/api/src/api_service/config.py, services/api/src/api_service/dependencies.py, services/api/src/api_service/schemas.py, services/api/src/api_service/repositories/operator_dashboard.py, services/api/src/api_service/services/operator_dashboard.py, services/api/src/api_service/main.py, services/api/tests/test_operator_dashboard.py</files>
  <read_first>services/api/src/api_service/config.py, services/api/src/api_service/dependencies.py, services/api/src/api_service/schemas.py, services/api/src/api_service/main.py, services/api/src/api_service/repositories/webhooks.py, .planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: `GET /internal/operator/jobs` returns counts for `queued`, `running`, `completed`, and `failed` plus filterable job-history rows with `client_id`, `current_stage`, `failure`, and webhook summary.
    - Test 2: `GET /internal/operator/jobs/{job_id}` returns stage events, latest extraction/classification model versions, latency signals, and webhook delivery attempt history for one job.
    - Test 3: Both routes require internal operator auth and are not callable with the public API key dependency.
  </behavior>
  <action>Add `OPERATOR_BEARER_TOKEN` and `OPERATOR_DASHBOARD_ENABLED` to `services/api/src/api_service/config.py`. Create a dependency `get_authenticated_operator(...)` in `services/api/src/api_service/dependencies.py` that requires `Authorization: Bearer <token>`. Extend `services/api/src/api_service/schemas.py` with `OperatorDashboardSummaryResponse`, `OperatorJobListItemResponse`, `OperatorJobDetailResponse`, `OperatorStageEventResponse`, and `OperatorWebhookDeliveryResponse`. Create `services/api/src/api_service/repositories/operator_dashboard.py` with SQLAlchemy queries that aggregate from `jobs`, `job_events`, `extraction_runs`, `classification_runs`, and `webhook_deliveries`. Create `services/api/src/api_service/services/operator_dashboard.py` with `get_operator_dashboard_summary(...)`, `list_operator_jobs(...)`, and `get_operator_job_detail(...)`. Add internal JSON routes in `services/api/src/api_service/main.py` at `GET /internal/operator/jobs` and `GET /internal/operator/jobs/{job_id}` with query params `status`, `client_id`, `q`, and `limit`. Cover auth and payload shape in `services/api/tests/test_operator_dashboard.py`.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src pytest services/api/tests/test_operator_dashboard.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/config.py` contains `OPERATOR_BEARER_TOKEN`; `services/api/src/api_service/main.py` contains `/internal/operator/jobs` and `/internal/operator/jobs/{job_id}`; `services/api/src/api_service/repositories/operator_dashboard.py` queries `webhook_deliveries`; the operator dashboard tests assert public API keys do not pass operator auth.</acceptance_criteria>
  <done>The API exposes protected internal data surfaces that operators can use to inspect job counts, stage progression, failure details, model versions, and webhook outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: Render the internal operator dashboard UI and update local setup docs</name>
  <files>services/api/src/api_service/templates/operator_dashboard.html, services/api/src/api_service/main.py, services/api/tests/test_operator_dashboard.py, docs/foundation/operator-dashboard.md, docs/foundation/local-development.md, README.md</files>
  <read_first>services/api/src/api_service/main.py, services/api/src/api_service/templates, services/api/tests/test_operator_dashboard.py, README.md, docs/foundation/local-development.md, .planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md</read_first>
  <acceptance_criteria>`services/api/src/api_service/templates/operator_dashboard.html` exists; `services/api/src/api_service/main.py` contains `GET /internal/operator/dashboard`; `docs/foundation/operator-dashboard.md` contains the internal route paths; `README.md` contains `OPERATOR_BEARER_TOKEN` and `/internal/operator/dashboard`; `docs/foundation/local-development.md` contains the operator verification flow.</acceptance_criteria>
  <action>Create `services/api/src/api_service/templates/operator_dashboard.html` as a server-rendered internal page that reads from the JSON endpoints added in Task 1 and shows four top-level count panels for queued/running/completed/failed jobs, a searchable job-history table, a stage-timeline panel, a failure-diagnostics panel, and a webhook-delivery history panel. Use a minimal no-framework approach with semantic HTML, CSS inside the template, and a small inline script that calls `/internal/operator/jobs` and `/internal/operator/jobs/{job_id}`; do not introduce a separate frontend build system for this internal surface. Add `GET /internal/operator/dashboard` in `services/api/src/api_service/main.py` and protect it with the operator auth dependency. Extend `services/api/tests/test_operator_dashboard.py` with a route-registration check and an auth-protected HTML response assertion. Add `docs/foundation/operator-dashboard.md` describing the dashboard routes, filters, and job drill-down fields. Update `docs/foundation/local-development.md` and `README.md` with the exact local setup steps for the operator surface: set `OPERATOR_BEARER_TOKEN`, start the stack, visit `/internal/operator/dashboard`, call the JSON endpoints with the bearer token, and verify webhook history and failure diagnostics appear for seeded/test jobs. This README update is mandatory per AGENTS.md whenever local setup or verification steps change.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src pytest services/api/tests/test_operator_dashboard.py services/api/tests/test_health.py -q</automated>
  </verify>
  <done>Internal operators have one protected dashboard route and companion drill-down APIs for diagnosing queue health, failed jobs, model lineage, and webhook delivery outcomes without manual database inspection.</done>
</task>

</tasks>

<verification>
Run the operator dashboard tests, then grep the new route definitions, template, README, and local-development docs for the internal dashboard path, bearer-token setup, and webhook/failure drill-down surfaces.
</verification>

<success_criteria>
Internal operators can use a protected dashboard to search jobs, inspect stage progression and failures, see model versions, and review webhook delivery history without direct database access.
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-delivery-and-operator-visibility/05-04-SUMMARY.md`
</output>
