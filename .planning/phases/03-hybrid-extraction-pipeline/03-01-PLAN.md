---
phase: 03-hybrid-extraction-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/doc_platform_contracts/extraction.py
  - packages/contracts/src/doc_platform_contracts/__init__.py
  - services/api/src/api_service/db/models.py
  - services/api/src/api_service/repositories/jobs.py
  - services/api/alembic/versions/0003_extraction_contract.py
  - services/api/tests/test_extraction_contract.py
  - docs/foundation/extraction-contract.md
autonomous: true
requirements:
  - EXT-06
must_haves:
  truths:
    - "Extraction outputs use one normalized extracted-text contract regardless of input format."
    - "Extraction metadata records page order, extraction path, and source artifact references for every completed extraction."
    - "Downstream stages can find extracted text artifacts without format-specific lookup rules."
  artifacts:
    - path: "packages/contracts/src/doc_platform_contracts/extraction.py"
      provides: "Canonical extracted-text payload and traceability metadata types"
    - path: "services/api/src/api_service/db/models.py"
      provides: "Durable extraction metadata tables linked to jobs and artifacts"
    - path: "services/api/alembic/versions/0003_extraction_contract.py"
      provides: "Schema migration for extraction lineage persistence"
    - path: "docs/foundation/extraction-contract.md"
      provides: "Human-readable contract for normalized extraction artifacts"
  key_links:
    - from: "packages/contracts/src/doc_platform_contracts/extraction.py"
      to: "services/api/src/api_service/db/models.py"
      via: "Persisted fields mirror the normalized traceability contract"
      pattern: "extraction_path|page_count|source_artifact"
    - from: "services/api/src/api_service/repositories/jobs.py"
      to: "services/api/src/api_service/db/models.py"
      via: "Repository helpers create extraction run and artifact lineage rows"
      pattern: "create_extraction|update_extraction"
---

<objective>
Define the normalized extraction contract and the durable metadata model Phase 3 builds on.

Purpose: Lock the shared extracted-text schema, traceability metadata, and Postgres persistence shape before extractor and orchestration work begins.
Output: Shared extraction contract types, extraction metadata tables/migration, repository helpers, and a foundation doc for downstream plans.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-hybrid-extraction-pipeline/03-CONTEXT.md
@.planning/phases/01-core-platform-foundation/01-02-SUMMARY.md
@.planning/phases/02-external-ingestion-contract/02-01-SUMMARY.md
@docs/foundation/storage-contract.md
@packages/contracts/src/doc_platform_contracts/storage_keys.py
@services/api/src/api_service/db/models.py
@services/api/src/api_service/repositories/jobs.py

<interfaces>
From `packages/contracts/src/doc_platform_contracts/storage_keys.py`:

```python
ArtifactType = Literal[
    "original",
    "page-image",
    "ocr-json",
    "extracted-text",
    "classification-result",
]

STORAGE_KEY_TEMPLATE = "tenants/{tenant_id}/jobs/{job_id}/{stage}/{artifact_type}/{filename}"
```

From `services/api/src/api_service/db/models.py`:

```python
class Job(Base):
    id: Mapped[str]
    document_id: Mapped[str]
    tenant_id: Mapped[str]
    status: Mapped[str]
    current_stage: Mapped[str]

class Artifact(Base):
    id: Mapped[str]
    job_id: Mapped[str]
    artifact_type: Mapped[str]
    stage: Mapped[str]
    storage_key: Mapped[str]
    metadata_json: Mapped[dict]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the normalized extraction contract and contract tests</name>
  <files>packages/contracts/src/doc_platform_contracts/extraction.py, packages/contracts/src/doc_platform_contracts/__init__.py, services/api/tests/test_extraction_contract.py, docs/foundation/extraction-contract.md</files>
  <read_first>packages/contracts/src/doc_platform_contracts/storage_keys.py, packages/contracts/src/doc_platform_contracts/__init__.py, docs/foundation/storage-contract.md, .planning/phases/03-hybrid-extraction-pipeline/03-CONTEXT.md, docs/superpowers/plans/2026-04-21-document-inference-platform-implementation-plan.md</read_first>
  <behavior>
    - Test 1: A normalized extraction payload for a native PDF contains `text`, `document_id`, `job_id`, `extraction_path`, `page_count`, `pages`, and `source_artifact_ids`.
    - Test 2: Page metadata preserves explicit `page_number`, ordered `text`, and `source_artifact_id`.
    - Test 3: Traceability metadata records `fallback_used`, `fallback_reason`, and `produced_by` fields for deterministic fallback behavior per D-07 and D-08.
  </behavior>
  <action>Create `packages/contracts/src/doc_platform_contracts/extraction.py` with explicit Pydantic or dataclass-based exported types for `ExtractionPage`, `ExtractionTrace`, and `ExtractedTextArtifact`. The contract must include concrete fields `job_id`, `document_id`, `tenant_id`, `source_media_type`, `extraction_path`, `fallback_used`, `fallback_reason`, `page_count`, `pages`, `text`, `source_artifact_ids`, `produced_by`, and `created_at` per D-03, D-04, D-07, and D-08. Export the new types from `packages/contracts/src/doc_platform_contracts/__init__.py`. Add `services/api/tests/test_extraction_contract.py` that asserts the contract round-trips digital and OCR-shaped payloads and that page ordering remains numeric and stable. Write `docs/foundation/extraction-contract.md` with the exact normalized JSON field names, the allowed `extraction_path` values `direct` and `ocr`, and the artifact expectation that both paths persist as `artifact_type="extracted-text"` under the shared storage namespace.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_extraction_contract.py -q</automated>
  </verify>
  <acceptance_criteria>`packages/contracts/src/doc_platform_contracts/extraction.py` exports `ExtractionPage`, `ExtractionTrace`, and `ExtractedTextArtifact`; `docs/foundation/extraction-contract.md` contains the exact strings `extraction_path`, `source_artifact_ids`, `fallback_used`, and `artifact_type="extracted-text"`; `services/api/tests/test_extraction_contract.py` contains assertions for both `direct` and `ocr` payloads.</acceptance_criteria>
  <done>The codebase has a single source-of-truth extracted-text contract that downstream services can import without inventing field names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist extraction lineage in the API-owned metadata schema</name>
  <files>services/api/src/api_service/db/models.py, services/api/src/api_service/repositories/jobs.py, services/api/alembic/versions/0003_extraction_contract.py, services/api/tests/test_extraction_contract.py, docs/foundation/extraction-contract.md</files>
  <read_first>services/api/src/api_service/db/models.py, services/api/src/api_service/repositories/jobs.py, services/api/alembic/versions/0002_ingestion_contract.py, services/api/tests/test_foundation_schema.py, services/api/tests/test_upload_api.py, packages/contracts/src/doc_platform_contracts/extraction.py</read_first>
  <behavior>
    - Test 1: The schema creates durable extraction lineage rows linked to a job and stores `extraction_path`, `fallback_used`, `page_count`, and `source_artifact_ids`.
    - Test 2: Repository helpers can create an extraction run plus the derived `extracted-text` artifact metadata without bypassing the API-owned models.
    - Test 3: The migration declares the new table names and columns explicitly so Alembic upgrades are deterministic.
  </behavior>
  <action>Extend `services/api/src/api_service/db/models.py` with a new extraction-specific table such as `ExtractionRun` keyed by `id` and `job_id`, with columns `stage`, `extraction_path`, `fallback_used`, `fallback_reason`, `page_count`, `source_artifact_ids_json`, `trace_json`, and timestamps. Add the corresponding relationship from `Job`. Update `services/api/src/api_service/repositories/jobs.py` with concrete helper functions `create_extraction_run(...)`, `get_extraction_run_for_job(...)`, and `create_extracted_text_artifact(...)` that write `artifact_type="extracted-text"` metadata using the normalized contract fields. Add `services/api/alembic/versions/0003_extraction_contract.py` with `revision = "0003_extraction_contract"` and explicit `upgrade()` / `downgrade()` logic for the new table and indexes. Extend `services/api/tests/test_extraction_contract.py` to create a job, write an extraction run, and assert the persisted values `direct`, `False`, and a numeric page count. Update `docs/foundation/extraction-contract.md` to document the Postgres-side persistence fields and how they relate to object storage artifacts.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_extraction_contract.py services/api/tests/test_foundation_schema.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/db/models.py` contains `__tablename__ = "extraction_runs"` and the fields `extraction_path`, `fallback_used`, `fallback_reason`, and `page_count`; `services/api/src/api_service/repositories/jobs.py` contains `create_extraction_run` and `create_extracted_text_artifact`; `services/api/alembic/versions/0003_extraction_contract.py` contains `revision = "0003_extraction_contract"` and `op.create_table("extraction_runs"`.</acceptance_criteria>
  <done>API-owned persistence can store normalized extraction lineage and derived extracted-text artifacts for downstream stages.</done>
</task>

</tasks>

<verification>
Run the contract and schema tests, then grep the new contract and migration for the normalized extraction fields and table names.
</verification>

<success_criteria>
Phase 3 has a durable extracted-text contract and metadata schema that explicitly capture page order, extraction path, fallback state, and source artifact references.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hybrid-extraction-pipeline/03-01-SUMMARY.md`
</output>
