---
phase: 02-external-ingestion-contract
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - services/api/src/api_service/auth.py
  - services/api/src/api_service/dependencies.py
  - services/api/src/api_service/repositories/jobs.py
  - services/api/src/api_service/repositories/clients.py
  - services/api/src/api_service/schemas.py
  - services/api/src/api_service/services/ingestion.py
  - services/api/src/api_service/main.py
  - services/orchestrator/src/orchestrator_service/celery_app.py
  - services/api/tests/test_upload_api.py
  - services/api/tests/test_auth_api_keys.py
  - docs/foundation/external-ingestion-api.md
autonomous: true
requirements: [ING-01, ING-02, ING-03, ING-04, ING-05, SEC-01]
user_setup: []
must_haves:
  truths:
    - "Authenticated clients can submit supported files through one multipart upload endpoint."
    - "Accepted uploads create one durable job and return stable `job_id` and `document_id` values."
    - "Repeated requests with the same idempotency key return the original accepted identifiers instead of duplicating work."
  artifacts:
    - "services/api/src/api_service/main.py"
    - "services/api/src/api_service/services/ingestion.py"
    - "services/api/src/api_service/auth.py"
    - "services/api/tests/test_upload_api.py"
    - "docs/foundation/external-ingestion-api.md"
  key_links:
    - "Request authentication depends on the static API-key config and client storage introduced in 02-01."
    - "Upload acceptance persists the raw file through the existing storage adapter and schedules downstream processing through the queue boundary."
---

<objective>
Implement the authenticated multipart upload contract with durable acceptance and idempotent retry behavior.

Purpose: This is the core client-facing Phase 2 surface: one protected upload endpoint that validates inputs, persists accepted uploads, and hands work to the async pipeline safely.
Output: Auth helpers, request/response schemas, upload handler/service flow, tests, and external API documentation for the upload contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-external-ingestion-contract/02-CONTEXT.md
@.planning/phases/02-external-ingestion-contract/02-01-PLAN.md
@docs/foundation/api-auth.md
@services/api/src/api_service/main.py
@services/api/src/api_service/config.py
@services/api/src/api_service/storage.py
@services/api/src/api_service/db/models.py
@services/orchestrator/src/orchestrator_service/celery_app.py
@system_design.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement API-key auth and multipart upload acceptance</name>
  <files>services/api/src/api_service/auth.py, services/api/src/api_service/dependencies.py, services/api/src/api_service/repositories/jobs.py, services/api/src/api_service/repositories/clients.py, services/api/src/api_service/schemas.py, services/api/src/api_service/services/ingestion.py, services/api/src/api_service/main.py, services/orchestrator/src/orchestrator_service/celery_app.py, services/api/tests/test_upload_api.py, services/api/tests/test_auth_api_keys.py</files>
  <read_first>.planning/phases/02-external-ingestion-contract/02-CONTEXT.md, .planning/REQUIREMENTS.md, services/api/src/api_service/main.py, services/api/src/api_service/config.py, services/api/src/api_service/storage.py, services/api/src/api_service/db/models.py, services/api/src/api_service/db/session.py, services/orchestrator/src/orchestrator_service/celery_app.py, docs/foundation/api-auth.md</read_first>
  <action>Implement the Phase 2 upload flow in the API service. Add `services/api/src/api_service/auth.py` with helpers that read the exact header configured by `API_KEY_HEADER_NAME`, compare against the configured `API_KEYS_JSON` client map or `api_clients` records, and raise `401 Unauthorized` for missing or invalid keys. Add `services/api/src/api_service/dependencies.py` to expose FastAPI dependencies for authenticated client resolution and database/storage access. Add repository modules `services/api/src/api_service/repositories/jobs.py` and `services/api/src/api_service/repositories/clients.py` so upload handlers do not write raw ORM queries inline. Add `services/api/src/api_service/schemas.py` with request/response models for an accepted upload response containing `job_id`, `document_id`, `status`, and `current_stage`, and error responses containing `error_code`, `message`, and optional `details`. Add `services/api/src/api_service/services/ingestion.py` that validates supported content types exactly as `application/pdf`, `image/png`, `image/jpeg`, `application/vnd.openxmlformats-officedocument.wordprocessingml.document`, `text/plain`, and `application/json`; rejects empty files and files larger than `MAX_UPLOAD_BYTES`; builds the raw object-storage key with artifact type `original`; persists the upload bytes through `StorageAdapter.put_artifact`; creates one `jobs` row plus at least one `job_events` row and one `artifacts` row; and returns the original accepted identifiers when the same authenticated client repeats the request with the same idempotency key. Update `services/api/src/api_service/main.py` to expose `POST /v1/documents:upload` as a multipart endpoint that accepts the file plus an `Idempotency-Key` header, calls the ingestion service, and enqueues the accepted job onto the existing orchestrator queue boundary using the upload/preprocess handoff you define explicitly in code. Update `services/orchestrator/src/orchestrator_service/celery_app.py` only as needed to expose a concrete task or queue send target for upload acceptance. Add tests in `services/api/tests/test_auth_api_keys.py` and `services/api/tests/test_upload_api.py` that verify: missing API key returns `401`; unsupported media types return a documented client error; accepted uploads return `job_id`, `document_id`, `status`, and `current_stage`; duplicate idempotent retries return the same identifiers; and raw accepted uploads write an artifact row with artifact type `original`.</action>
  <verify>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_auth_api_keys.py services/api/tests/test_upload_api.py -q</verify>
  <acceptance_criteria>
    - `services/api/src/api_service/main.py` contains `@app.post("/v1/documents:upload")`
    - `services/api/src/api_service/auth.py` contains `API_KEY_HEADER_NAME`
    - `services/api/src/api_service/services/ingestion.py` contains `Idempotency-Key`
    - `services/api/src/api_service/services/ingestion.py` contains `application/pdf`, `image/png`, `image/jpeg`, `application/vnd.openxmlformats-officedocument.wordprocessingml.document`, `text/plain`, and `application/json`
    - `services/api/tests/test_upload_api.py` asserts the same `job_id` and `document_id` are returned for duplicate idempotency requests
    - `services/api/tests/test_auth_api_keys.py` asserts `401` for missing API keys
  </acceptance_criteria>
  <done>The platform exposes a real protected upload endpoint that durably accepts work and handles duplicate retries safely.</done>
</task>

<task type="auto">
  <name>Task 2: Document the external upload contract</name>
  <files>docs/foundation/external-ingestion-api.md</files>
  <read_first>.planning/phases/02-external-ingestion-contract/02-CONTEXT.md, docs/foundation/api-auth.md, services/api/src/api_service/schemas.py, services/api/src/api_service/main.py</read_first>
  <action>Create `docs/foundation/external-ingestion-api.md` documenting the upload API with the exact route `POST /v1/documents:upload`, authentication via API key header, multipart file submission, required `Idempotency-Key` header, the accepted response shape fields `job_id`, `document_id`, `status`, and `current_stage`, and error envelopes with `error_code` and `message`. Include one example for a successful PDF upload and one example for an unsupported media type rejection.</action>
  <verify>rg -n "POST /v1/documents:upload|Idempotency-Key|job_id|document_id|error_code|message" docs/foundation/external-ingestion-api.md</verify>
  <acceptance_criteria>
    - `docs/foundation/external-ingestion-api.md` contains `POST /v1/documents:upload`
    - `docs/foundation/external-ingestion-api.md` contains `Idempotency-Key`
    - `docs/foundation/external-ingestion-api.md` contains `job_id`
    - `docs/foundation/external-ingestion-api.md` contains `document_id`
    - `docs/foundation/external-ingestion-api.md` contains `error_code`
  </acceptance_criteria>
  <done>The upload contract is explicit for integrators and consistent with the implemented API behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_auth_api_keys.py services/api/tests/test_upload_api.py -q`
- [ ] `rg -n "@app.post\\(\"/v1/documents:upload\"\\)|Idempotency-Key|application/pdf|application/json" services/api/src/api_service/main.py services/api/src/api_service/services/ingestion.py`
- [ ] `rg -n "401|job_id|document_id|current_stage|error_code" services/api/tests/test_auth_api_keys.py services/api/tests/test_upload_api.py docs/foundation/external-ingestion-api.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Authenticated clients can submit supported files through one durable, idempotent multipart upload contract
</success_criteria>

<output>
After completion, create `.planning/phases/02-external-ingestion-contract/02-02-SUMMARY.md`
</output>
