---
phase: 06-reliability-and-input-safety
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - services/api/src/api_service/repositories/jobs.py
  - services/api/src/api_service/services/status.py
  - services/api/src/api_service/services/results.py
  - services/api/src/api_service/services/operator_dashboard.py
  - services/api/src/api_service/schemas.py
  - services/api/tests/test_status_api.py
  - services/api/tests/test_results_api.py
  - services/api/tests/test_operator_dashboard.py
  - docs/foundation/status-polling-contract.md
  - docs/foundation/results-contract.md
  - docs/foundation/local-development.md
  - README.md
autonomous: true
requirements:
  - SEC-03
  - SEC-04
must_haves:
  truths:
    - "Worker restarts and retries preserve durable job consistency and do not expose results before required artifacts are persisted."
    - "Client and operator surfaces show stable failure categories and poison/dead-letter visibility without leaking verbose internal diagnostics to public APIs."
    - "Local setup and verification docs explain how to validate Phase 6 behavior, including unsafe inputs, retry exhaustion, and dead-letter outcomes."
  artifacts:
    - path: "services/api/src/api_service/services/status.py"
      provides: "Stable public failure/status shaping for retrying and dead-lettered jobs"
    - path: "services/api/src/api_service/services/operator_dashboard.py"
      provides: "Operator visibility into poison/dead-letter and delivery exhaustion state"
    - path: "services/api/tests/test_results_api.py"
      provides: "Guards that results remain unavailable until durable extracted/classified artifacts exist"
    - path: "README.md"
      provides: "Updated local setup and Phase 6 verification instructions as required by AGENTS.md"
  key_links:
    - from: "services/api/src/api_service/services/results.py"
      to: "services/api/src/api_service/repositories/jobs.py"
      via: "Results readiness still depends on durable artifacts and must remain safe under retries and worker restarts"
      pattern: "results_not_ready|artifact"
    - from: "services/api/src/api_service/services/status.py"
      to: "services/api/src/api_service/services/operator_dashboard.py"
      via: "Public and operator failure views share stable categories while operator detail remains richer"
      pattern: "failure|dead_letter|poison"
---

<objective>
Close Phase 6 by making public/operator visibility and local verification consistent with the new reliability contract.

Purpose: Finish D-13 through D-18 by ensuring restart-safe durable state is reflected correctly in status/results/operator surfaces and by updating local setup docs so the new failure behaviors can be reproduced and checked.
Output: Status/results/operator updates, reliability-focused tests, and mandatory README/local-development refresh for Phase 6 verification.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reliability-and-input-safety/06-CONTEXT.md
@.planning/phases/06-reliability-and-input-safety/06-01-PLAN.md
@.planning/phases/06-reliability-and-input-safety/06-02-PLAN.md
@services/api/src/api_service/repositories/jobs.py
@services/api/src/api_service/services/status.py
@services/api/src/api_service/services/results.py
@services/api/src/api_service/services/operator_dashboard.py
@services/api/src/api_service/schemas.py
@services/api/tests/test_status_api.py
@services/api/tests/test_results_api.py
@services/api/tests/test_operator_dashboard.py
@docs/foundation/status-polling-contract.md
@docs/foundation/results-contract.md
@docs/foundation/local-development.md
@README.md

<interfaces>
From `services/api/src/api_service/services/results.py`:

```python
if job.status != "completed" or job.current_stage != "classified":
    raise APIError(
        status_code=409,
        error_code="results_not_ready",
        message="Results are not ready for this job.",
    )
```

From `services/api/src/api_service/services/status.py`:

```python
if job.status == "failed" and job.failure_code and job.failure_message:
    failure = FailureResponse(code=job.failure_code, message=job.failure_message)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make public status and results contracts reliability-safe under retries and dead-letter outcomes</name>
  <files>services/api/src/api_service/repositories/jobs.py, services/api/src/api_service/services/status.py, services/api/src/api_service/services/results.py, services/api/src/api_service/schemas.py, services/api/tests/test_status_api.py, services/api/tests/test_results_api.py, docs/foundation/status-polling-contract.md, docs/foundation/results-contract.md</files>
  <read_first>services/api/src/api_service/repositories/jobs.py, services/api/src/api_service/services/status.py, services/api/src/api_service/services/results.py, services/api/src/api_service/schemas.py, services/api/tests/test_status_api.py, services/api/tests/test_results_api.py, .planning/phases/06-reliability-and-input-safety/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Public job status exposes stable failure codes/categories for unsafe input, transient-upstream exhaustion, and poison/dead-letter outcomes.
    - Test 2: Retrying or dead-lettered jobs do not surface final results until required extracted-text and classification artifacts are durably present.
    - Test 3: Completed jobs with exhausted webhook delivery still return results normally while operator/public delivery metadata remains separate.
  </behavior>
  <action>Extend `services/api/src/api_service/schemas.py` so status responses can carry the stable failure categories introduced in Plan 02 while preserving the existing lifecycle-focused shape. In `services/api/src/api_service/services/status.py`, map dead-letter and reliability fields from `Job` into exact public failure codes such as `unsafe_input_type_mismatch`, `encrypted_pdf`, `corrupt_pdf`, `invalid_image_encoding`, `transient_upstream_exhausted`, `poison_job`, and `webhook_delivery_exhausted`, but keep the payload bounded to `code` and `message`. In `services/api/src/api_service/services/results.py`, preserve the exact rule that results are only available when `job.status == "completed"` and both extracted and classified artifacts plus `classification_run` exist; add explicit tests in `services/api/tests/test_results_api.py` showing that retrying/dead-lettered jobs and partially persisted jobs still return `results_not_ready`, while completed jobs with failed webhook deliveries still return normal results. Update `docs/foundation/status-polling-contract.md` and `docs/foundation/results-contract.md` with the exact failure-code categories and the statement that delivery exhaustion does not invalidate already-persisted completed results.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src pytest services/api/tests/test_status_api.py services/api/tests/test_results_api.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/services/status.py` contains `poison_job` and `webhook_delivery_exhausted`; `services/api/tests/test_results_api.py` asserts completed jobs with failed webhook delivery still return results; `docs/foundation/results-contract.md` states delivery exhaustion does not invalidate completed results.</acceptance_criteria>
  <done>Public lifecycle and results APIs stay durable and conservative even while retries, dead-letter states, and delivery exhaustion are happening behind the scenes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose reliability state to operators and refresh local setup/readme verification steps</name>
  <files>services/api/src/api_service/services/operator_dashboard.py, services/api/src/api_service/schemas.py, services/api/tests/test_operator_dashboard.py, docs/foundation/local-development.md, README.md</files>
  <read_first>services/api/src/api_service/services/operator_dashboard.py, services/api/src/api_service/schemas.py, services/api/tests/test_operator_dashboard.py, docs/foundation/local-development.md, README.md, .planning/phases/06-reliability-and-input-safety/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Operator job detail shows retry count, dead-letter reason, and terminal failure category for poison jobs.
    - Test 2: Operator views distinguish core job completion from webhook delivery exhaustion.
    - Test 3: Local setup docs explain how to reproduce Phase 6 unsafe-input and retry/dead-letter behavior on a local stack.
  </behavior>
  <action>Extend `services/api/src/api_service/schemas.py` and `services/api/src/api_service/services/operator_dashboard.py` so operator list/detail responses include exact fields `retry_count`, `max_retry_count`, `dead_lettered_at`, `dead_letter_reason`, and `terminal_failure_category`, plus a webhook-delivery summary that can show `failed` without implying the core job itself failed. Update `services/api/tests/test_operator_dashboard.py` with fixture jobs covering unsafe-input terminal failure, transient retry exhaustion, dead-letter state, and completed-with-webhook-failure state. Update `docs/foundation/local-development.md` with a new Phase 6 verification section containing exact commands for the reliability-focused suites: `pytest services/api/tests/test_upload_api.py services/api/tests/test_status_api.py services/api/tests/test_results_api.py -q`, `pytest services/extractor/tests/test_extraction_service.py -q`, and `pytest services/orchestrator/tests/test_extraction_tasks.py services/orchestrator/tests/test_classification_tasks.py services/orchestrator/tests/test_webhook_tasks.py -q` using the existing `PYTEST_DISABLE_PLUGIN_AUTOLOAD` and `PYTHONPATH` prefixes. Update `README.md` to add a dedicated Phase 6 section under local setup that explains the conservative input-safety rules, the three-window webhook retry behavior, the dead-letter/poison outcomes operators should look for, and the exact local verification commands. This README update is mandatory per AGENTS.md whenever local setup or verification steps change.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src pytest services/api/tests/test_operator_dashboard.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/services/operator_dashboard.py` contains `dead_letter_reason` and `terminal_failure_category`; `services/api/tests/test_operator_dashboard.py` covers completed-with-webhook-failure and dead-lettered jobs; `docs/foundation/local-development.md` contains a Phase 6 verification section; `README.md` contains `Phase 6` and mentions the three webhook retry windows.</acceptance_criteria>
  <done>Operators can inspect retry/dead-letter outcomes clearly, and the repo docs show exactly how to run and verify the new Phase 6 reliability behavior locally.</done>
</task>

</tasks>

<verification>
Run the API status/results/operator suites, then grep status, operator, local-development, and README for `poison_job`, `webhook_delivery_exhausted`, `dead_letter_reason`, and the Phase 6 verification commands.
</verification>

<success_criteria>
Restart-safe durable state is reflected consistently in public and operator surfaces, and the local setup docs explain how to validate unsafe-input, retry, and dead-letter behavior end to end.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-and-input-safety/06-03-SUMMARY.md`
</output>
