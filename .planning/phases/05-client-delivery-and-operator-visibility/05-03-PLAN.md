---
phase: 05-client-delivery-and-operator-visibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/doc_platform_contracts/observability.py
  - services/api/src/api_service/observability.py
  - services/api/src/api_service/main.py
  - services/orchestrator/src/orchestrator_service/observability.py
  - services/orchestrator/src/orchestrator_service/tasks.py
  - services/extractor/src/extractor_service/observability.py
  - services/extractor/src/extractor_service/main.py
  - services/classifier/src/classifier_service/observability.py
  - services/classifier/src/classifier_service/main.py
  - services/api/tests/test_observability.py
  - services/orchestrator/tests/test_observability.py
  - .env.example
  - docs/foundation/observability.md
autonomous: true
requirements:
  - OPS-05
must_haves:
  truths:
    - "API, worker, extraction, classification, and webhook-delivery paths emit structured logs, aggregate metrics, and trace spans."
    - "Per-job detail is available in traces/logs while metric labels stay bounded and do not use raw job IDs as labels."
    - "Operators can correlate failures across services through shared trace and log fields without manual database inspection."
  artifacts:
    - path: "packages/contracts/src/doc_platform_contracts/observability.py"
      provides: "Shared observability configuration and field names"
    - path: "services/api/src/api_service/observability.py"
      provides: "API request metrics/log/tracing hooks"
    - path: "services/orchestrator/src/orchestrator_service/observability.py"
      provides: "Worker and webhook-delivery metrics/log/tracing hooks"
    - path: "docs/foundation/observability.md"
      provides: "Metric names, trace span names, and required env vars"
  key_links:
    - from: "services/api/src/api_service/main.py"
      to: "services/api/src/api_service/observability.py"
      via: "FastAPI app installs request instrumentation middleware"
      pattern: "observability"
    - from: "services/orchestrator/src/orchestrator_service/tasks.py"
      to: "services/orchestrator/src/orchestrator_service/observability.py"
      via: "Task execution records counters, latency, and trace context for queue work"
      pattern: "observe|trace|metric"
---

<objective>
Add production-grade observability across the Phase 5 platform surfaces without overloading metrics with per-job cardinality.

Purpose: Satisfy OPS-05 and D-10 through D-12 by standardizing metrics, structured logs, and per-job tracing across the API, workers, inference services, and webhook delivery path.
Output: Shared observability helpers, service-level instrumentation points, tests, env vars, and an observability reference doc.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md
@system_design.txt
@document_inference_platform_done_checklist.md
@services/api/src/api_service/main.py
@services/orchestrator/src/orchestrator_service/tasks.py
@services/extractor/src/extractor_service/main.py
@services/classifier/src/classifier_service/main.py
@packages/contracts/src/doc_platform_contracts/settings.py

<interfaces>
From `packages/contracts/src/doc_platform_contracts/settings.py`:

```python
class BasePlatformSettings(BaseSettings):
    postgres_dsn: str
    rabbitmq_url: str
    object_storage_endpoint: str
    log_level: str
```

From `system_design.txt`:

```text
metrics for aggregates
traces for per-job detail
logs for event context
DB for business state
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a shared observability contract with bounded metric labels and correlation fields</name>
  <files>packages/contracts/src/doc_platform_contracts/observability.py, services/api/src/api_service/observability.py, services/orchestrator/src/orchestrator_service/observability.py, services/extractor/src/extractor_service/observability.py, services/classifier/src/classifier_service/observability.py, services/api/tests/test_observability.py, services/orchestrator/tests/test_observability.py, .env.example, docs/foundation/observability.md</files>
  <read_first>packages/contracts/src/doc_platform_contracts/settings.py, system_design.txt, document_inference_platform_done_checklist.md, services/api/src/api_service/main.py, services/orchestrator/src/orchestrator_service/tasks.py, .planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Shared helpers expose the exact correlation fields `job_id`, `document_id`, `tenant_id`, `current_stage`, and `trace_id` for structured logs.
    - Test 2: Metrics use bounded labels such as `service`, `stage`, `outcome`, and `event_type`, and explicitly do not use raw `job_id` or `document_id` as metric labels.
    - Test 3: Trace span helpers standardize span names for `api.request`, `orchestrator.task`, `extractor.run`, `classifier.run`, and `webhook.deliver`.
  </behavior>
  <action>Create `packages/contracts/src/doc_platform_contracts/observability.py` with shared constants and helper functions for correlation field names, trace span names, and metric names. Add per-service modules `services/api/src/api_service/observability.py`, `services/orchestrator/src/orchestrator_service/observability.py`, `services/extractor/src/extractor_service/observability.py`, and `services/classifier/src/classifier_service/observability.py` that configure OpenTelemetry tracing/metrics and JSON structured logging via the standard library `logging` module. Standardize these metric names: `doc_platform_api_requests_total`, `doc_platform_job_stage_failures_total`, `doc_platform_queue_tasks_total`, `doc_platform_extraction_latency_seconds`, `doc_platform_classification_latency_seconds`, `doc_platform_webhook_delivery_attempts_total`, and `doc_platform_job_duration_seconds`. Add `.env.example` keys `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_SERVICE_NAMESPACE`, and `TRACE_SAMPLE_RATIO`. Document the exact correlation fields, span names, and metric names in `docs/foundation/observability.md`. Add tests that assert metric-name registration and that forbidden high-cardinality labels such as `job_id` are not part of the metric-label definition.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src:services/orchestrator/src:services/extractor/src:services/classifier/src pytest services/api/tests/test_observability.py services/orchestrator/tests/test_observability.py -q</automated>
  </verify>
  <acceptance_criteria>`packages/contracts/src/doc_platform_contracts/observability.py` exists; `.env.example` contains `OTEL_EXPORTER_OTLP_ENDPOINT` and `TRACE_SAMPLE_RATIO`; `docs/foundation/observability.md` contains `doc_platform_webhook_delivery_attempts_total`; the new observability tests assert `job_id` is not a metric label.</acceptance_criteria>
  <done>The repo has one shared observability vocabulary for metrics, logs, and traces that all runtime surfaces can use consistently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Instrument API, worker, extractor, and classifier execution paths</name>
  <files>services/api/src/api_service/main.py, services/orchestrator/src/orchestrator_service/tasks.py, services/extractor/src/extractor_service/main.py, services/classifier/src/classifier_service/main.py, services/api/src/api_service/observability.py, services/orchestrator/src/orchestrator_service/observability.py, services/extractor/src/extractor_service/observability.py, services/classifier/src/classifier_service/observability.py, services/api/tests/test_observability.py, services/orchestrator/tests/test_observability.py</files>
  <read_first>services/api/src/api_service/main.py, services/orchestrator/src/orchestrator_service/tasks.py, services/extractor/src/extractor_service/main.py, services/classifier/src/classifier_service/main.py, services/api/src/api_service/observability.py, services/orchestrator/src/orchestrator_service/observability.py</read_first>
  <behavior>
    - Test 1: API requests emit request counters and request spans around `/v1/documents:upload`, `/v1/jobs/{job_id}`, and `/v1/jobs/{job_id}/results`.
    - Test 2: Orchestrator tasks emit queue-task counters plus per-stage success/failure timing, including the reserved webhook queue path.
    - Test 3: Extractor and classifier endpoints emit stage latency spans and structured logs that include correlation fields from the request payload.
  </behavior>
  <action>Wire the new observability helpers into `services/api/src/api_service/main.py` using FastAPI middleware or dependency hooks so public API requests emit counters/histograms and request spans with bounded labels `route`, `method`, and `status_family`. Instrument `services/orchestrator/src/orchestrator_service/tasks.py` so extraction, classification, and webhook tasks log `job_id`, `document_id`, and `tenant_id`, increment queue counters, record duration histograms, and emit failure counters by `stage` and `outcome`. Instrument `services/extractor/src/extractor_service/main.py` and `services/classifier/src/classifier_service/main.py` so inference requests create spans and latency metrics named from Task 1. Ensure the webhook task instrumentation reserves `event_type` and `delivery_status` as labels rather than `job_id`. Extend the new observability tests to assert the middleware/hooks are installed and that task wrappers are applied around the orchestrator task functions.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src:services/orchestrator/src:services/extractor/src:services/classifier/src pytest services/api/tests/test_observability.py services/orchestrator/tests/test_observability.py services/api/tests/test_health.py services/orchestrator/tests/test_celery_app.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/main.py` imports or calls observability setup; `services/orchestrator/src/orchestrator_service/tasks.py` contains instrumentation for `document.webhook`; `services/extractor/src/extractor_service/main.py` and `services/classifier/src/classifier_service/main.py` import their observability helpers; tests assert webhook delivery instrumentation exists.</acceptance_criteria>
  <done>Core runtime paths emit the metrics, logs, and trace spans operators need to diagnose failures without using job IDs as metric labels.</done>
</task>

</tasks>

<verification>
Run the observability-focused test files plus health/task smoke tests, then grep the shared contract, runtime entrypoints, and docs for the exact metric names, trace span names, and OTEL env vars.
</verification>

<success_criteria>
The platform emits bounded-label metrics, correlated structured logs, and end-to-end trace spans across API, worker, inference, and webhook-delivery execution paths.
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-delivery-and-operator-visibility/05-03-SUMMARY.md`
</output>
