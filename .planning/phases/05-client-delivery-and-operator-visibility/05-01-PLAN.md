---
phase: 05-client-delivery-and-operator-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/alembic/versions/0005_webhook_delivery_contract.py
  - services/api/src/api_service/db/models.py
  - services/api/src/api_service/repositories/webhooks.py
  - services/api/src/api_service/schemas.py
  - services/api/src/api_service/services/webhooks.py
  - services/api/tests/test_webhook_contract.py
  - docs/foundation/webhook-contract.md
autonomous: true
requirements:
  - DLV-04
  - DLV-05
must_haves:
  truths:
    - "Each client integration can have a durable webhook configuration instead of passing a callback URL on every upload."
    - "Webhook payloads for terminal states are signed and contain the identifiers, final status, inline summary, and canonical results reference clients need."
    - "Webhook delivery attempts have durable records that later plans can retry, inspect, and surface to operators."
  artifacts:
    - path: "services/api/alembic/versions/0005_webhook_delivery_contract.py"
      provides: "Webhook subscription and delivery-attempt schema"
    - path: "services/api/src/api_service/services/webhooks.py"
      provides: "Terminal-state webhook payload builder and signature helper"
    - path: "services/api/tests/test_webhook_contract.py"
      provides: "Contract tests for signed payload shape and storage metadata"
    - path: "docs/foundation/webhook-contract.md"
      provides: "Documented webhook headers, payload fields, and retry expectations"
  key_links:
    - from: "services/api/src/api_service/services/webhooks.py"
      to: "services/api/src/api_service/services/results.py"
      via: "Webhook payload builder reuses the canonical results reference instead of replacing it"
      pattern: "results"
    - from: "services/api/src/api_service/repositories/webhooks.py"
      to: "services/api/src/api_service/db/models.py"
      via: "Repository persists webhook subscriptions and delivery attempts"
      pattern: "Webhook"
---

<objective>
Define the durable webhook delivery contract before any async dispatch logic is added.

Purpose: Lock the data model and payload/signing rules so Phase 5 delivery work stays client-level, terminal-state-only, and traceable back to the canonical results API per D-01 through D-06.
Output: Webhook schema migration, API models/repositories/services for payload assembly, contract tests, and a webhook contract doc.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md
@docs/foundation/status-polling-contract.md
@docs/foundation/results-contract.md
@services/api/src/api_service/db/models.py
@services/api/src/api_service/repositories/jobs.py
@services/api/src/api_service/schemas.py
@services/api/src/api_service/services/results.py

<interfaces>
From `services/api/src/api_service/schemas.py`:

```python
class JobResultsResponse(BaseModel):
    job_id: str
    document_id: str
    status: str
    extracted_text: str
    classification: ClassificationMetadataResponse
    artifacts: list[ArtifactReferenceResponse]
    completed_at: datetime
```

From `services/api/src/api_service/db/models.py`:

```python
class Job(Base):
    id: Mapped[str]
    document_id: Mapped[str]
    tenant_id: Mapped[str]
    client_id: Mapped[str | None]
    status: Mapped[str]
    current_stage: Mapped[str]
    failure_code: Mapped[str | None]
    failure_message: Mapped[str | None]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the durable webhook subscription and delivery-attempt schema</name>
  <files>services/api/alembic/versions/0005_webhook_delivery_contract.py, services/api/src/api_service/db/models.py, services/api/src/api_service/repositories/webhooks.py, services/api/tests/test_webhook_contract.py</files>
  <read_first>services/api/src/api_service/db/models.py, services/api/src/api_service/repositories/jobs.py, services/api/alembic/versions/0004_classification_results.py, docs/foundation/results-contract.md, .planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A webhook subscription is persisted against `client_id` with one callback URL and signing secret per integration, not per upload request, per D-01.
    - Test 2: A webhook delivery attempt record stores the terminal `event_type`, payload snapshot, attempt number, HTTP result, and next retry time needed for bounded retries per D-02 and D-03.
    - Test 3: Delivery history is queryable by `job_id` and `client_id` for later operator drill-downs.
  </behavior>
  <action>Create Alembic migration `0005_webhook_delivery_contract.py` that adds two new API-owned tables: `webhook_subscriptions` and `webhook_deliveries`. `webhook_subscriptions` must contain `id`, `client_id`, `target_url`, `signing_secret`, `subscribed_events_json`, `is_active`, `created_at`, and `updated_at`, with an index on `client_id`. `webhook_deliveries` must contain `id`, `job_id`, `client_id`, `subscription_id`, `event_type`, `payload_json`, `delivery_status`, `attempt_count`, `last_http_status`, `last_error_message`, `next_retry_at`, `last_attempt_at`, `created_at`, and `updated_at`, with indexes on `job_id`, `client_id`, and `delivery_status`. Extend `services/api/src/api_service/db/models.py` with ORM models `WebhookSubscription` and `WebhookDelivery` plus relationships from `Job` where useful. Create `services/api/src/api_service/repositories/webhooks.py` with concrete helpers `get_active_webhook_subscription_for_client(...)`, `create_webhook_delivery(...)`, `update_webhook_delivery_attempt(...)`, and `list_webhook_deliveries_for_job(...)`. Add `services/api/tests/test_webhook_contract.py` that validates the migration table names and repository behavior using the in-memory SQLAlchemy test pattern already used in API tests.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src pytest services/api/tests/test_webhook_contract.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/alembic/versions/0005_webhook_delivery_contract.py` contains `webhook_subscriptions` and `webhook_deliveries`; `services/api/src/api_service/db/models.py` contains `class WebhookSubscription` and `class WebhookDelivery`; `services/api/src/api_service/repositories/webhooks.py` contains `create_webhook_delivery` and `list_webhook_deliveries_for_job`; `services/api/tests/test_webhook_contract.py` asserts both new table names.</acceptance_criteria>
  <done>The platform has durable webhook configuration and delivery-attempt storage that later dispatch and dashboard plans can build on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the signed terminal-state webhook payload builder and contract doc</name>
  <files>services/api/src/api_service/schemas.py, services/api/src/api_service/services/webhooks.py, services/api/tests/test_webhook_contract.py, docs/foundation/webhook-contract.md</files>
  <read_first>services/api/src/api_service/schemas.py, services/api/src/api_service/services/results.py, docs/foundation/results-contract.md, docs/foundation/status-polling-contract.md, .planning/phases/05-client-delivery-and-operator-visibility/05-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Completed-job payloads contain `job_id`, `document_id`, `status`, `results_url`, and an inline `result_summary` with classification label, confidence, and artifact references per D-05 and D-06.
    - Test 2: Failed-job payloads contain the same stable identifiers plus `failure.code` and `failure.message` while still using the same terminal event envelope per D-02.
    - Test 3: Signing uses an HMAC-SHA256 over the raw JSON payload and emits a deterministic header value for the same secret and payload per D-04.
  </behavior>
  <action>Extend `services/api/src/api_service/schemas.py` with explicit webhook contract models named `WebhookFailureResponse`, `WebhookResultSummaryResponse`, and `TerminalWebhookPayload`. Create `services/api/src/api_service/services/webhooks.py` with `build_terminal_webhook_payload(...)` and `sign_webhook_payload(...)`. `TerminalWebhookPayload` must expose the exact top-level fields `event_type`, `job_id`, `document_id`, `client_id`, `tenant_id`, `status`, `current_stage`, `results_url`, `result_summary`, `failure`, and `occurred_at`. The `event_type` must be `job.completed` or `job.failed`, per D-02. The `results_url` must point to `/v1/jobs/{job_id}/results` for completed jobs and remain present as the durable canonical reference per D-06. The inline `result_summary` for completed jobs must include `final_label`, `confidence`, `low_confidence_policy`, `model`, `version`, and `artifact_types`; failed jobs must set `result_summary` to `null` and populate `failure`. Implement signing to output the exact header value `sha256=<hex digest>` intended for `X-DocPlatform-Signature`. Update `docs/foundation/webhook-contract.md` with the exact header name, the event names, the payload field list, and the rule that webhook payloads are terminal-state-only and reference the canonical results endpoint instead of replacing it.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src pytest services/api/tests/test_webhook_contract.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/schemas.py` contains `TerminalWebhookPayload`; `services/api/src/api_service/services/webhooks.py` contains `build_terminal_webhook_payload` and `sign_webhook_payload`; `docs/foundation/webhook-contract.md` contains `X-DocPlatform-Signature`, `job.completed`, `job.failed`, and `/v1/jobs/{job_id}/results`.</acceptance_criteria>
  <done>The webhook payload and signing contract is locked and documented before any dispatcher or operator surface depends on it.</done>
</task>

</tasks>

<verification>
Run the webhook contract test suite, then grep the migration, service, and doc files for the exact table names, payload model names, signature header, and results reference path.
</verification>

<success_criteria>
Webhook delivery is defined as a client-level, terminal-state, signed contract with durable configuration and delivery-attempt storage that supports later retries and operator visibility.
</success_criteria>

<output>
After completion, create `.planning/phases/05-client-delivery-and-operator-visibility/05-01-SUMMARY.md`
</output>
