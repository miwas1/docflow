---
phase: 01-core-platform-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - packages/contracts/src/doc_platform_contracts/models.py
  - packages/contracts/src/doc_platform_contracts/storage_keys.py
  - services/api/src/api_service/db/base.py
  - services/api/src/api_service/db/models.py
  - services/api/src/api_service/db/session.py
  - services/api/src/api_service/storage.py
  - services/api/alembic.ini
  - services/api/alembic/env.py
  - services/api/alembic/versions/0001_foundation_schema.py
  - services/api/tests/test_storage_keys.py
  - services/api/tests/test_foundation_schema.py
  - docs/foundation/storage-contract.md
autonomous: true
requirements: [OPS-01, OPS-02]
user_setup: []
must_haves:
  truths:
    - "Postgres persistence for jobs, job events, artifacts, and model versions exists before business pipeline logic begins."
    - "Object-storage keys are standardized by tenant, job, stage, and artifact type in one namespace strategy."
    - "The storage and metadata contracts are documented clearly enough for later ingestion and extraction phases to reuse without schema drift."
  artifacts:
    - "services/api/alembic/versions/0001_foundation_schema.py"
    - "services/api/src/api_service/db/models.py"
    - "packages/contracts/src/doc_platform_contracts/storage_keys.py"
    - "docs/foundation/storage-contract.md"
  key_links:
    - "The Alembic migration and SQLAlchemy models describe the same tables and key columns."
    - "The storage adapter and storage key helper encode the same tenant/job/stage artifact naming strategy described in the documentation."
---

<objective>
Define the durable metadata schema and object-storage contract for the platform foundation.

Purpose: Later pipeline phases need a stable persistence model for jobs, events, artifacts, and model/version traceability.
Output: Shared domain models, database schema migration, storage-key conventions, adapter stubs, and documentation for the storage contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-platform-foundation/01-CONTEXT.md
@system_design.txt
@document_inference_platform_done_checklist.md
@services/api/src/api_service/config.py
@packages/contracts/src/doc_platform_contracts/settings.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 1 metadata schema and shared persistence models</name>
  <files>packages/contracts/src/doc_platform_contracts/models.py, services/api/src/api_service/db/base.py, services/api/src/api_service/db/models.py, services/api/src/api_service/db/session.py, services/api/alembic.ini, services/api/alembic/env.py, services/api/alembic/versions/0001_foundation_schema.py, services/api/tests/test_foundation_schema.py</files>
  <read_first>.planning/phases/01-core-platform-foundation/01-CONTEXT.md, .planning/REQUIREMENTS.md, system_design.txt, document_inference_platform_done_checklist.md, services/api/src/api_service/config.py, packages/contracts/src/doc_platform_contracts/settings.py</read_first>
  <action>Implement the metadata foundation in the API service as the initial owner of the shared persistence contract. Create SQLAlchemy base/session wiring and define tables named `jobs`, `job_events`, `artifacts`, and `model_versions`. Use concrete columns: `jobs` must include `id`, `document_id`, `tenant_id`, `status`, `current_stage`, `source_filename`, `source_media_type`, `storage_key`, `created_at`, and `updated_at`; `job_events` must include `id`, `job_id`, `event_type`, `stage`, `payload_json`, and `created_at`; `artifacts` must include `id`, `job_id`, `artifact_type`, `stage`, `storage_key`, `media_type`, `metadata_json`, and `created_at`; `model_versions` must include `id`, `model_family`, `version`, `routing_policy`, `rollout_bucket`, and `created_at`. Add an Alembic foundation migration named `0001_foundation_schema.py` that creates the same tables and indexes for `jobs.status`, `jobs.current_stage`, `job_events.job_id`, and `artifacts.job_id`. Add a schema-focused test that asserts the migration metadata includes those exact table names and that the ORM models expose the same table names.</action>
  <verify>pytest services/api/tests/test_foundation_schema.py -q</verify>
  <acceptance_criteria>
    - `services/api/src/api_service/db/models.py` contains `__tablename__ = "jobs"`
    - `services/api/src/api_service/db/models.py` contains `__tablename__ = "job_events"`
    - `services/api/src/api_service/db/models.py` contains `__tablename__ = "artifacts"`
    - `services/api/src/api_service/db/models.py` contains `__tablename__ = "model_versions"`
    - `services/api/alembic/versions/0001_foundation_schema.py` contains `create_table("jobs"` 
    - `services/api/alembic/versions/0001_foundation_schema.py` contains `create_index`
  </acceptance_criteria>
  <done>The foundation schema exists in both migration and ORM form and supports Phase 1 persistence goals.</done>
</task>

<task type="auto">
  <name>Task 2: Define storage key conventions and storage adapter stubs</name>
  <files>packages/contracts/src/doc_platform_contracts/storage_keys.py, services/api/src/api_service/storage.py, services/api/tests/test_storage_keys.py, docs/foundation/storage-contract.md</files>
  <read_first>.planning/phases/01-core-platform-foundation/01-CONTEXT.md, system_design.txt, document_inference_platform_done_checklist.md, packages/contracts/src/doc_platform_contracts/models.py, services/api/src/api_service/db/models.py</read_first>
  <action>Create a shared storage-key helper that encodes the single-bucket logical namespace strategy using the exact pattern `tenants/{tenant_id}/jobs/{job_id}/{stage}/{artifact_type}/{filename}`. Support artifact types `original`, `page-image`, `ocr-json`, `extracted-text`, and `classification-result`. In `services/api/src/api_service/storage.py`, add a storage adapter stub class that accepts endpoint, bucket, access key, and secret key from settings and exposes methods `put_artifact`, `get_artifact_uri`, and `build_storage_key` using the shared helper. Document the contract in `docs/foundation/storage-contract.md`, including which artifacts live in object storage, which metadata stays in Postgres, and how later phases should map digital extraction outputs and OCR outputs into the same artifact namespace. Add tests asserting the exact key format and artifact type tokens so later phases cannot silently change the naming strategy.</action>
  <verify>pytest services/api/tests/test_storage_keys.py -q && rg -n "tenants/\\{tenant_id\\}/jobs/\\{job_id\\}/\\{stage\\}/\\{artifact_type\\}/\\{filename\\}" docs/foundation/storage-contract.md</verify>
  <acceptance_criteria>
    - `packages/contracts/src/doc_platform_contracts/storage_keys.py` contains `tenants/{tenant_id}/jobs/{job_id}/{stage}/{artifact_type}/{filename}`
    - `packages/contracts/src/doc_platform_contracts/storage_keys.py` contains `original`, `page-image`, `ocr-json`, `extracted-text`, and `classification-result`
    - `services/api/src/api_service/storage.py` contains `def put_artifact`
    - `services/api/src/api_service/storage.py` contains `def get_artifact_uri`
    - `docs/foundation/storage-contract.md` contains `Postgres stores metadata` and `Object storage stores blobs`
  </acceptance_criteria>
  <done>The artifact namespace and storage adapter contract are fixed for later phases and backed by tests and docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest services/api/tests/test_foundation_schema.py services/api/tests/test_storage_keys.py -q`
- [ ] `rg -n "__tablename__ = \"jobs\"|__tablename__ = \"job_events\"|__tablename__ = \"artifacts\"|__tablename__ = \"model_versions\"" services/api/src/api_service/db/models.py`
- [ ] `rg -n "original|page-image|ocr-json|extracted-text|classification-result" packages/contracts/src/doc_platform_contracts/storage_keys.py docs/foundation/storage-contract.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The platform has a durable schema and storage contract ready for later ingestion and extraction phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-platform-foundation/01-02-SUMMARY.md`
</output>
