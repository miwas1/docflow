---
phase: 02-external-ingestion-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - Makefile
  - packages/contracts/src/doc_platform_contracts/models.py
  - packages/contracts/src/doc_platform_contracts/settings.py
  - services/api/src/api_service/config.py
  - services/api/src/api_service/db/models.py
  - services/api/src/api_service/db/session.py
  - services/api/alembic/env.py
  - services/api/alembic/versions/0002_ingestion_contract.py
  - services/api/tests/test_ingestion_schema.py
  - docs/foundation/api-auth.md
autonomous: true
requirements: [SEC-01, ING-05]
user_setup: []
must_haves:
  truths:
    - "Phase 2 has a concrete static API-key authentication contract for external clients."
    - "The metadata model can persist upload requests, idempotency keys, and authenticated client identity before async processing begins."
    - "The database migration and ORM models describe the same ingestion-related fields and tables."
  artifacts:
    - "services/api/alembic/versions/0002_ingestion_contract.py"
    - "services/api/src/api_service/db/models.py"
    - "services/api/src/api_service/config.py"
    - "docs/foundation/api-auth.md"
  key_links:
    - "API settings expose auth-related configuration that later request handlers can consume."
    - "The migration extends the Phase 1 persistence contract with the same ingestion/auth fields the ORM uses."
---

<objective>
Establish the authentication and persistence foundation for the external ingestion contract.

Purpose: Phase 2 needs a concrete static API-key model and durable ingestion metadata before upload and polling endpoints can be implemented safely.
Output: Auth/config contract, ingestion-oriented schema extensions, shared model fields, and a short authentication reference document.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-external-ingestion-contract/02-CONTEXT.md
@.planning/phases/01-core-platform-foundation/01-CONTEXT.md
@services/api/src/api_service/db/models.py
@services/api/src/api_service/config.py
@services/api/src/api_service/db/session.py
@system_design.txt
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add static API key and ingestion persistence fields</name>
  <files>packages/contracts/src/doc_platform_contracts/models.py, packages/contracts/src/doc_platform_contracts/settings.py, services/api/src/api_service/config.py, services/api/src/api_service/db/models.py, services/api/src/api_service/db/session.py, services/api/alembic/env.py, services/api/alembic/versions/0002_ingestion_contract.py, services/api/tests/test_ingestion_schema.py</files>
  <read_first>.planning/phases/02-external-ingestion-contract/02-CONTEXT.md, .planning/REQUIREMENTS.md, services/api/src/api_service/db/models.py, services/api/src/api_service/config.py, services/api/src/api_service/db/session.py, services/api/alembic/versions/0001_foundation_schema.py, packages/contracts/src/doc_platform_contracts/settings.py, packages/contracts/src/doc_platform_contracts/models.py</read_first>
  <action>Extend the API service persistence contract for Phase 2. Add static API-key settings to `packages/contracts/src/doc_platform_contracts/settings.py` and `services/api/src/api_service/config.py` with exact configuration names `API_KEY_HEADER_NAME`, `API_KEYS_JSON`, and `MAX_UPLOAD_BYTES`. Update `services/api/src/api_service/db/models.py` so `jobs` includes nullable fields `client_id`, `idempotency_key`, `failure_code`, and `failure_message`, and add a new table `api_clients` with columns `id`, `client_id`, `display_name`, `api_key_hash`, `is_active`, `created_at`, and `updated_at`. Mirror those changes in a new Alembic migration `services/api/alembic/versions/0002_ingestion_contract.py`, including indexes for `jobs.idempotency_key`, `jobs.client_id`, and unique constraints on `api_clients.client_id`. Update `packages/contracts/src/doc_platform_contracts/models.py` with matching dataclass records for the new fields/table, and ensure `services/api/src/api_service/db/session.py` still exports reusable engine/session helpers after the schema extension. Add `services/api/tests/test_ingestion_schema.py` that asserts the ORM exposes `api_clients`, the `jobs` table has `client_id`, `idempotency_key`, `failure_code`, and `failure_message` columns, and the migration metadata includes those exact table/column names.</action>
  <verify>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_ingestion_schema.py -q</verify>
  <acceptance_criteria>
    - `packages/contracts/src/doc_platform_contracts/settings.py` contains `API_KEY_HEADER_NAME`, `API_KEYS_JSON`, and `MAX_UPLOAD_BYTES`
    - `services/api/src/api_service/db/models.py` contains `__tablename__ = "api_clients"`
    - `services/api/src/api_service/db/models.py` contains `idempotency_key`
    - `services/api/src/api_service/db/models.py` contains `failure_code`
    - `services/api/alembic/versions/0002_ingestion_contract.py` contains `create_table("api_clients"`
    - `services/api/alembic/versions/0002_ingestion_contract.py` contains `idempotency_key`
  </acceptance_criteria>
  <done>The API service can represent authenticated clients and ingestion lifecycle metadata before implementing request handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Document the static API key contract</name>
  <files>docs/foundation/api-auth.md</files>
  <read_first>.planning/phases/02-external-ingestion-contract/02-CONTEXT.md, .planning/REQUIREMENTS.md, services/api/src/api_service/config.py, packages/contracts/src/doc_platform_contracts/settings.py</read_first>
  <action>Create `docs/foundation/api-auth.md` documenting the Phase 2 authentication contract. State explicitly that external clients authenticate with static API keys, the request header name comes from `API_KEY_HEADER_NAME`, inactive or unknown keys must produce `401 Unauthorized`, and the contract does not include JWT or OAuth in Phase 2. Include a short example request showing `X-API-Key: <key>` and note that later phases may add rotation or more advanced auth without changing the v1 protected-ingestion contract.</action>
  <verify>rg -n "static API keys|API_KEY_HEADER_NAME|401 Unauthorized|X-API-Key" docs/foundation/api-auth.md</verify>
  <acceptance_criteria>
    - `docs/foundation/api-auth.md` contains `static API keys`
    - `docs/foundation/api-auth.md` contains `API_KEY_HEADER_NAME`
    - `docs/foundation/api-auth.md` contains `401 Unauthorized`
    - `docs/foundation/api-auth.md` contains `X-API-Key`
  </acceptance_criteria>
  <done>The Phase 2 auth choice is captured in a single document downstream agents and operators can reference.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_ingestion_schema.py -q`
- [ ] `rg -n "__tablename__ = \"api_clients\"|idempotency_key|failure_code|failure_message|client_id" services/api/src/api_service/db/models.py`
- [ ] `rg -n "API_KEY_HEADER_NAME|API_KEYS_JSON|MAX_UPLOAD_BYTES" packages/contracts/src/doc_platform_contracts/settings.py services/api/src/api_service/config.py`
- [ ] `rg -n "static API keys|401 Unauthorized|X-API-Key" docs/foundation/api-auth.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repository has a durable auth and ingestion-persistence foundation that matches the locked Phase 2 decisions
</success_criteria>

<output>
After completion, create `.planning/phases/02-external-ingestion-contract/02-01-SUMMARY.md`
</output>
