---
phase: 06-reliability-and-input-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/src/api_service/config.py
  - services/api/src/api_service/schemas.py
  - services/api/src/api_service/services/ingestion.py
  - services/api/tests/test_upload_api.py
  - services/extractor/src/extractor_service/extraction.py
  - services/extractor/tests/test_extraction_service.py
  - docs/foundation/external-ingestion-api.md
  - docs/foundation/hybrid-extraction-pipeline.md
  - docs/foundation/local-development.md
autonomous: true
requirements:
  - SEC-02
must_haves:
  truths:
    - "Unsafe input handling validates the real file signature instead of trusting only the declared MIME type or extension."
    - "Corrupt PDFs, encrypted PDFs, spoofed file types, and invalid image encodings fail with stable terminal input-safety reasons instead of entering blind retry loops."
    - "Unsafe-input jobs retain the original upload for traceability but do not produce derived extraction or classification artifacts."
  artifacts:
    - path: "services/api/src/api_service/services/ingestion.py"
      provides: "Upload-time signature validation and unsafe-input rejection policy"
    - path: "services/extractor/src/extractor_service/extraction.py"
      provides: "Stage-level validation and extractor error taxonomy for corrupt or encrypted inputs"
    - path: "services/api/tests/test_upload_api.py"
      provides: "API tests for spoofed types, encrypted PDFs, and terminal unsafe-input failures"
    - path: "services/extractor/tests/test_extraction_service.py"
      provides: "Extractor tests for corrupt PDFs, invalid images, and encrypted-PDF handling"
    - path: "docs/foundation/external-ingestion-api.md"
      provides: "Documented upload validation and failure-code contract"
  key_links:
    - from: "services/api/src/api_service/services/ingestion.py"
      to: "services/extractor/src/extractor_service/extraction.py"
      via: "Upload-time file-signature policy aligns with extractor-stage content validation and error codes"
      pattern: "unsupported_media_type|unsafe_input|encrypted"
    - from: "services/api/tests/test_upload_api.py"
      to: "docs/foundation/external-ingestion-api.md"
      via: "Tests enforce the same documented unsafe-input and rejection contract presented to clients"
      pattern: "unsupported|encrypted|spoof"
---

<objective>
Define and implement the Phase 6 unsafe-input contract before retry/dead-letter logic is expanded.

Purpose: Satisfy SEC-02 and decisions D-01 through D-05 by enforcing signature-based upload validation, extractor-side corrupt/encrypted checks, and stable terminal failure semantics for bad inputs.
Output: Concrete ingestion/extractor validation logic, stable unsafe-input error codes, tests, and updated input-safety documentation for local verification.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-reliability-and-input-safety/06-CONTEXT.md
@docs/foundation/external-ingestion-api.md
@docs/foundation/hybrid-extraction-pipeline.md
@services/api/src/api_service/config.py
@services/api/src/api_service/schemas.py
@services/api/src/api_service/services/ingestion.py
@services/extractor/src/extractor_service/extraction.py
@services/api/tests/test_upload_api.py
@services/extractor/tests/test_extraction_service.py

<interfaces>
From `services/api/src/api_service/services/ingestion.py`:

```python
SUPPORTED_CONTENT_TYPES = {
    "application/pdf",
    "image/png",
    "image/jpeg",
    "application/vnd.openxmlformats-officedocument.wordprocessingml.document",
    "text/plain",
    "application/json",
}
```

From `services/extractor/src/extractor_service/extraction.py`:

```python
class ExtractionError(Exception):
    def __init__(self, *, error_code: str, message: str, status_code: int = 400) -> None:
        ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce signature-based upload validation and stable unsafe-input API failures</name>
  <files>services/api/src/api_service/config.py, services/api/src/api_service/schemas.py, services/api/src/api_service/services/ingestion.py, services/api/tests/test_upload_api.py, docs/foundation/external-ingestion-api.md</files>
  <read_first>services/api/src/api_service/config.py, services/api/src/api_service/schemas.py, services/api/src/api_service/services/ingestion.py, services/api/tests/test_upload_api.py, docs/foundation/external-ingestion-api.md, .planning/phases/06-reliability-and-input-safety/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Upload acceptance verifies the actual file signature for PDF, PNG, JPEG, DOCX/ZIP, TXT, and JSON instead of trusting only `UploadFile.content_type`.
    - Test 2: A mismatched declared type versus detected type is rejected with a terminal unsafe-input error before any async work is enqueued.
    - Test 3: Encrypted PDFs are rejected with a dedicated stable failure code and do not create derived artifacts.
  </behavior>
  <action>Add explicit upload-validation settings in `services/api/src/api_service/config.py` for `INPUT_SIGNATURE_VALIDATION_ENABLED` defaulting to `true` and `UNSAFE_INPUT_REJECT_MISMATCH` defaulting to `true`. Extend `services/api/src/api_service/schemas.py` with a reusable failure-code vocabulary if needed so public status/docs can use the exact codes `unsafe_input_type_mismatch`, `encrypted_pdf`, and `invalid_input_payload`. In `services/api/src/api_service/services/ingestion.py`, add concrete helpers that inspect magic bytes and content structure: PDF starts with `%PDF-`, PNG starts with `\x89PNG\r\n\x1a\n`, JPEG starts with `\xff\xd8`, DOCX is a ZIP archive containing `word/document.xml`, JSON decodes with `json.loads`, and TXT decodes as UTF-8. Reject uploads when `upload_file.content_type` is supported but the detected type differs and `UNSAFE_INPUT_REJECT_MISMATCH` is true. For PDFs, detect encryption by scanning the first bytes for `/Encrypt` and reject with `APIError(status_code=422, error_code="encrypted_pdf", message="Encrypted PDFs are not supported.")`. Ensure successful unsafe-input rejection happens before `storage.put_artifact(...)`, `create_job(...)`, `create_job_event(...)`, `create_artifact(...)`, or `enqueue_upload_job(...)`. Add tests in `services/api/tests/test_upload_api.py` for spoofed media type mismatch, encrypted PDF rejection, valid PDF acceptance, and proof that rejected uploads do not persist a `Job` or derived `Artifact`. Update `docs/foundation/external-ingestion-api.md` with the exact validation rule, stable error codes, and the statement that unsupported or unsafe inputs fail before async processing begins.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/api/src pytest services/api/tests/test_upload_api.py services/api/tests/test_status_api.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/services/ingestion.py` contains magic-byte or ZIP-based validation for PDF/PNG/JPEG/DOCX plus `encrypted_pdf`; `services/api/tests/test_upload_api.py` covers spoofed type mismatch and encrypted PDF rejection; `docs/foundation/external-ingestion-api.md` contains `unsafe_input_type_mismatch` and `encrypted_pdf`.</acceptance_criteria>
  <done>The upload surface enforces conservative signature-based validation and stable terminal unsafe-input failures before async processing begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add extractor-stage corrupt-input and invalid-image policy with stable terminal errors</name>
  <files>services/extractor/src/extractor_service/extraction.py, services/extractor/tests/test_extraction_service.py, docs/foundation/hybrid-extraction-pipeline.md, docs/foundation/local-development.md</files>
  <read_first>services/extractor/src/extractor_service/extraction.py, services/extractor/src/extractor_service/main.py, services/extractor/tests/test_extraction_service.py, docs/foundation/hybrid-extraction-pipeline.md, .planning/phases/06-reliability-and-input-safety/06-CONTEXT.md</read_first>
  <behavior>
    - Test 1: Corrupt or structurally invalid PDFs fail with a dedicated terminal extractor error instead of silently falling back to OCR.
    - Test 2: Invalid image encodings fail terminal with a stable error code and are not retried by default.
    - Test 3: OCR-path placeholder behavior remains only for valid image/PDF inputs that legitimately require OCR, not for corrupted content.
  </behavior>
  <action>In `services/extractor/src/extractor_service/extraction.py`, split unsafe-content validation from routing. Add helpers `validate_pdf_content(...)` and `validate_image_content(...)`. `validate_pdf_content(...)` must require `%PDF-` at the start, reject `/Encrypt`, and raise `ExtractionError(error_code="corrupt_pdf", message="PDF content is corrupt or unreadable.", status_code=422)` when the content is too malformed to parse. `validate_image_content(...)` must recognize PNG by `\x89PNG\r\n\x1a\n` and JPEG by `\xff\xd8`, rejecting anything else with `ExtractionError(error_code="invalid_image_encoding", message="Image content is corrupt or has an invalid encoding.", status_code=422)`. Update `run_extraction(...)` so valid PDFs still route through `extract_pdf(...)`, valid text-like formats still parse directly, and only validated images/PDF OCR fallbacks reach `build_ocr_placeholder(...)`. Add tests in `services/extractor/tests/test_extraction_service.py` for corrupt PDF rejection, encrypted-PDF rejection, invalid image rejection, and preservation of the existing direct-PDF and OCR-fallback happy paths. Update `docs/foundation/hybrid-extraction-pipeline.md` and `docs/foundation/local-development.md` with the exact extractor failure codes `corrupt_pdf`, `encrypted_pdf`, and `invalid_image_encoding`, plus a targeted verification command for the extraction suite.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 PYTHONPATH=packages/contracts/src:services/extractor/src pytest services/extractor/tests/test_health.py services/extractor/tests/test_extraction_service.py -q</automated>
  </verify>
  <acceptance_criteria>`services/extractor/src/extractor_service/extraction.py` contains `corrupt_pdf` and `invalid_image_encoding`; `services/extractor/tests/test_extraction_service.py` covers invalid images and encrypted/corrupt PDFs; `docs/foundation/hybrid-extraction-pipeline.md` documents the exact failure codes.</acceptance_criteria>
  <done>The extractor no longer treats corrupted or encrypted content as normal OCR candidates and instead emits stable terminal unsafe-input failures.</done>
</task>

</tasks>

<verification>
Run the upload and extractor safety-focused test suites, then grep the ingestion, extractor, and docs files for `encrypted_pdf`, `unsafe_input_type_mismatch`, `corrupt_pdf`, and `invalid_image_encoding`.
</verification>

<success_criteria>
Bad inputs are rejected conservatively, with signature-based validation at ingestion and stable terminal corrupt/encrypted failure handling in extraction, while original unsafe uploads remain traceable without producing derived artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/06-reliability-and-input-safety/06-01-SUMMARY.md`
</output>
