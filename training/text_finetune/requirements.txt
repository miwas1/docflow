# Training-time dependencies for text-only fine-tuning.
# Use a GPU-enabled torch build on GPU machines; this is intentionally unpinned.
datasets>=2.20,<3.0
evaluate>=0.4,<1.0
huggingface_hub>=0.31,<1.0
numpy>=1.26
scikit-learn>=1.4,<2.0
torch>=2.8,<3.0
transformers>=4.52,<5.0
