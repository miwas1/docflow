---
phase: 04-classification-and-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/contracts/src/doc_platform_contracts/classification.py
  - packages/contracts/src/doc_platform_contracts/__init__.py
  - services/api/src/api_service/db/models.py
  - services/api/src/api_service/repositories/jobs.py
  - services/api/alembic/versions/0004_classification_results.py
  - services/api/tests/test_results_contract.py
  - docs/foundation/results-contract.md
autonomous: true
requirements:
  - CLS-02
  - CLS-04
  - DLV-03
must_haves:
  truths:
    - "Classification results use one shared contract that carries final label, confidence, candidate labels, and classifier version metadata."
    - "Durable persistence stores classification outputs separately from extracted-text artifacts without breaking existing artifact lookup rules."
    - "Jobs do not become completed until classification artifacts and metadata are durably recorded."
  artifacts:
    - path: "packages/contracts/src/doc_platform_contracts/classification.py"
      provides: "Canonical document-classification payload and candidate-label types"
    - path: "services/api/src/api_service/db/models.py"
      provides: "Durable classification metadata tables linked to jobs and artifacts"
    - path: "services/api/alembic/versions/0004_classification_results.py"
      provides: "Schema migration for classifier result persistence"
    - path: "docs/foundation/results-contract.md"
      provides: "Human-readable results contract for downstream API and service work"
  key_links:
    - from: "packages/contracts/src/doc_platform_contracts/classification.py"
      to: "services/api/src/api_service/db/models.py"
      via: "Persisted classifier metadata mirrors the shared classification contract fields"
      pattern: "final_label|confidence|model_version|candidate"
    - from: "services/api/src/api_service/repositories/jobs.py"
      to: "services/api/src/api_service/db/models.py"
      via: "Repository helpers persist classification runs, artifacts, and terminal job state"
      pattern: "create_classification|record_classification"
---

<objective>
Define the shared classification/results contract and the durable metadata model Phase 4 builds on.

Purpose: Lock the fixed-taxonomy classification payload, candidate-label metadata, and Postgres/object-storage persistence shape before classifier-service and API result-surface work begins.
Output: Shared classification contract types, classification metadata tables/migration, repository helpers, and a results contract document for downstream plans.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-classification-and-results/04-CONTEXT.md
@.planning/phases/03-hybrid-extraction-pipeline/03-01-SUMMARY.md
@.planning/phases/03-hybrid-extraction-pipeline/03-03-SUMMARY.md
@docs/foundation/extraction-contract.md
@docs/foundation/storage-contract.md
@packages/contracts/src/doc_platform_contracts/extraction.py
@services/api/src/api_service/db/models.py
@services/api/src/api_service/repositories/jobs.py

<interfaces>
From `packages/contracts/src/doc_platform_contracts/extraction.py`:

```python
class ExtractedTextArtifact(BaseModel):
    job_id: str
    document_id: str
    tenant_id: str
    source_media_type: str
    extraction_path: str
    text: str
```

From `services/api/src/api_service/db/models.py`:

```python
class Artifact(Base):
    artifact_type: Mapped[str]
    stage: Mapped[str]
    storage_key: Mapped[str]
    metadata_json: Mapped[dict]

class ModelVersion(Base):
    model_family: Mapped[str]
    version: Mapped[str]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared classification payload and contract tests</name>
  <files>packages/contracts/src/doc_platform_contracts/classification.py, packages/contracts/src/doc_platform_contracts/__init__.py, services/api/tests/test_results_contract.py, docs/foundation/results-contract.md</files>
  <read_first>packages/contracts/src/doc_platform_contracts/extraction.py, packages/contracts/src/doc_platform_contracts/__init__.py, docs/foundation/extraction-contract.md, docs/foundation/storage-contract.md, .planning/phases/04-classification-and-results/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A classification payload includes `job_id`, `document_id`, `tenant_id`, `final_label`, `confidence`, `candidate_labels`, `low_confidence_policy`, and `produced_by`.
    - Test 2: Candidate labels preserve ordered `label` and `score` pairs for diagnostic use without changing the final public label semantics.
    - Test 3: The contract explicitly supports `unknown/other` as a final label and records classifier model version metadata.
  </behavior>
  <action>Create `packages/contracts/src/doc_platform_contracts/classification.py` with exported Pydantic models `ClassificationCandidate`, `ClassifierTrace`, and `DocumentClassificationResult`. The payload must use the exact fields `job_id`, `document_id`, `tenant_id`, `final_label`, `confidence`, `candidate_labels`, `low_confidence_policy`, `threshold_applied`, `produced_by`, and `created_at`. `final_label` must allow the concrete taxonomy values `invoice`, `receipt`, `bank_statement`, `id_card`, `utility_bill`, `contract`, `medical_record`, `tax_form`, and `unknown_other`. Export the new types from `packages/contracts/src/doc_platform_contracts/__init__.py`. Add `services/api/tests/test_results_contract.py` asserting the contract round-trips a supported label and an `unknown_other` low-confidence result with ordered candidate labels. Write `docs/foundation/results-contract.md` with the exact field names, the final-label taxonomy mapping, the rule that low-confidence documents resolve publicly to `unknown/other`, and the expectation that completed results include extracted text, classification output, and artifact references.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_results_contract.py -q</automated>
  </verify>
  <acceptance_criteria>`packages/contracts/src/doc_platform_contracts/classification.py` exports `ClassificationCandidate`, `ClassifierTrace`, and `DocumentClassificationResult`; `docs/foundation/results-contract.md` contains the exact strings `final_label`, `candidate_labels`, `low_confidence_policy`, and `unknown/other`; `services/api/tests/test_results_contract.py` asserts both a supported-label payload and a low-confidence `unknown_other` payload.</acceptance_criteria>
  <done>The codebase has a single source-of-truth classification contract that both the classifier service and the API results surface can import.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist classification lineage and final result artifacts in the API-owned schema</name>
  <files>services/api/src/api_service/db/models.py, services/api/src/api_service/repositories/jobs.py, services/api/alembic/versions/0004_classification_results.py, services/api/tests/test_results_contract.py, docs/foundation/results-contract.md</files>
  <read_first>services/api/src/api_service/db/models.py, services/api/src/api_service/repositories/jobs.py, services/api/alembic/versions/0003_extraction_contract.py, services/api/tests/test_extraction_contract.py, packages/contracts/src/doc_platform_contracts/classification.py, .planning/phases/04-classification-and-results/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: The schema creates durable classification lineage rows linked to a job and stores `final_label`, `confidence`, `low_confidence_policy`, and classifier trace metadata.
    - Test 2: Repository helpers create a `classification-result` artifact metadata row and advance the job to `completed` only after persistence succeeds.
    - Test 3: The migration declares the new table names and columns explicitly so Alembic upgrades are deterministic.
  </behavior>
  <action>Extend `services/api/src/api_service/db/models.py` with a new `ClassificationRun` table keyed by `id` and `job_id`, with columns `stage`, `final_label`, `confidence`, `low_confidence_policy`, `threshold_applied`, `candidate_labels_json`, `trace_json`, and timestamps. Add the corresponding `classification_runs` relationship from `Job`. Update `services/api/src/api_service/repositories/jobs.py` with concrete helper functions `create_classification_run(...)`, `get_classification_run_for_job(...)`, `create_classification_result_artifact(...)`, and `record_classification_completion(...)`. `record_classification_completion(...)` must accept the normalized classification payload plus a `storage_key`, write `artifact_type="classification-result"` metadata, and set `job.current_stage = "classified"` and `job.status = "completed"` only after the classification rows are flushed. Add `services/api/alembic/versions/0004_classification_results.py` with `revision = "0004_classification_results"` and explicit `upgrade()` / `downgrade()` logic for the new table and indexes. Extend `services/api/tests/test_results_contract.py` to create a job fixture, persist a classification run, and assert the stored values `final_label == "invoice"`, a numeric `confidence`, and candidate-label JSON content. Update `docs/foundation/results-contract.md` to document the Postgres-side persistence fields and the `classification-result` artifact metadata shape.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_results_contract.py services/api/tests/test_foundation_schema.py -q</automated>
  </verify>
  <acceptance_criteria>`services/api/src/api_service/db/models.py` contains `__tablename__ = "classification_runs"` and the fields `final_label`, `confidence`, `low_confidence_policy`, and `candidate_labels_json`; `services/api/src/api_service/repositories/jobs.py` contains `create_classification_run` and `record_classification_completion`; `services/api/alembic/versions/0004_classification_results.py` contains `revision = "0004_classification_results"` and `op.create_table("classification_runs"`.</acceptance_criteria>
  <done>API-owned persistence can store classifier lineage and final classification artifacts without marking jobs complete too early.</done>
</task>

</tasks>

<verification>
Run the contract and schema tests, then grep the new contract and migration for the classification fields, taxonomy values, and table names.
</verification>

<success_criteria>
Phase 4 has a durable classification contract and metadata schema that explicitly capture final label, confidence, candidate labels, and classifier version traceability.
</success_criteria>

<output>
After completion, create `.planning/phases/04-classification-and-results/04-01-SUMMARY.md`
</output>
