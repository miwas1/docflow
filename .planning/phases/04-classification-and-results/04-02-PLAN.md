---
phase: 04-classification-and-results
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - services/classifier/pyproject.toml
  - services/classifier/src/classifier_service/config.py
  - services/classifier/src/classifier_service/inference.py
  - services/classifier/src/classifier_service/main.py
  - services/classifier/tests/test_health.py
  - services/classifier/tests/test_inference_service.py
  - services/orchestrator/src/orchestrator_service/classifier_client.py
  - services/orchestrator/src/orchestrator_service/tasks.py
  - services/orchestrator/tests/test_classification_tasks.py
autonomous: true
requirements:
  - CLS-01
  - CLS-02
  - CLS-03
  - CLS-04
must_haves:
  truths:
    - "Every extracted document resolves to a supported v1 label or `unknown/other` through a deterministic classifier-service contract."
    - "Low-confidence handling is explicit and stable, not inferred from missing values."
    - "Classifier outputs are traceable to a concrete model/version identifier and can be persisted downstream."
  artifacts:
    - path: "services/classifier/src/classifier_service/inference.py"
      provides: "Baseline classifier logic and low-confidence policy enforcement"
    - path: "services/classifier/src/classifier_service/main.py"
      provides: "HTTP classification surface for orchestrator calls"
    - path: "services/orchestrator/src/orchestrator_service/classifier_client.py"
      provides: "Classifier HTTP client wrapper for async orchestration"
    - path: "services/orchestrator/tests/test_classification_tasks.py"
      provides: "Behavior tests for classification request shaping and failure surfacing"
  key_links:
    - from: "services/classifier/src/classifier_service/main.py"
      to: "services/classifier/src/classifier_service/inference.py"
      via: "HTTP handler delegates to the shared classification runner"
      pattern: "run_classification"
    - from: "services/orchestrator/src/orchestrator_service/tasks.py"
      to: "services/orchestrator/src/orchestrator_service/classifier_client.py"
      via: "Celery tasks call the classifier service over HTTP"
      pattern: "run_classification_request"
---

<objective>
Implement the baseline classifier service and wire extracted-text classification through the async control plane.

Purpose: Turn the classifier boundary into a real Phase 4 service that maps extracted text to the fixed taxonomy, applies the low-confidence policy, and exposes a concrete HTTP surface for orchestration.
Output: Classifier inference module, classifier HTTP endpoint, orchestrator client/tasks for classification, and tests for taxonomy, confidence, and failure behavior.
</objective>

<execution_context>
@/home/devnexx/.codex/get-shit-done/workflows/execute-plan.md
@/home/devnexx/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-classification-and-results/04-CONTEXT.md
@.planning/phases/04-classification-and-results/04-01-PLAN.md
@packages/contracts/src/doc_platform_contracts/classification.py
@packages/contracts/src/doc_platform_contracts/extraction.py
@services/classifier/src/classifier_service/main.py
@services/classifier/src/classifier_service/config.py
@services/orchestrator/src/orchestrator_service/tasks.py
@services/orchestrator/src/orchestrator_service/extractor_client.py

<interfaces>
From `packages/contracts/src/doc_platform_contracts/classification.py`:

```python
class DocumentClassificationResult(BaseModel):
    job_id: str
    document_id: str
    tenant_id: str
    final_label: str
    confidence: float
```

From `packages/contracts/src/doc_platform_contracts/extraction.py`:

```python
class ExtractedTextArtifact(BaseModel):
    job_id: str
    document_id: str
    tenant_id: str
    text: str
    source_media_type: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the baseline classifier service with fixed-taxonomy output and low-confidence handling</name>
  <files>services/classifier/src/classifier_service/inference.py, services/classifier/src/classifier_service/config.py, services/classifier/src/classifier_service/main.py, services/classifier/pyproject.toml, services/classifier/tests/test_inference_service.py, services/classifier/tests/test_health.py</files>
  <read_first>services/classifier/src/classifier_service/main.py, services/classifier/src/classifier_service/config.py, services/classifier/tests/test_health.py, packages/contracts/src/doc_platform_contracts/classification.py, packages/contracts/src/doc_platform_contracts/extraction.py, .planning/phases/04-classification-and-results/04-CONTEXT.md</read_first>
  <behavior>
    - Test 1: A representative extracted-text payload returns a supported taxonomy label plus a numeric confidence score.
    - Test 2: A low-confidence or unsupported payload resolves publicly to `unknown_other` with an explicit low-confidence policy field.
    - Test 3: The classifier response includes candidate labels and classifier trace metadata with a concrete model version string.
  </behavior>
  <action>Create `services/classifier/src/classifier_service/inference.py` with a `run_classification(...)` entry point that accepts a normalized extracted-text payload or equivalent request model and returns `DocumentClassificationResult`. Implement a pragmatic baseline classifier using deterministic lexical rules or another simple open source-friendly baseline that recognizes the exact final-label set `invoice`, `receipt`, `bank_statement`, `id_card`, `utility_bill`, `contract`, `medical_record`, `tax_form`, and `unknown_other`. Add explicit threshold configuration in `services/classifier/src/classifier_service/config.py` using concrete env aliases `CLASSIFIER_MODEL_NAME`, `CLASSIFIER_MODEL_VERSION`, and `CLASSIFIER_CONFIDENCE_THRESHOLD`. Update `services/classifier/src/classifier_service/main.py` to add `POST /v1/classifications:run`, validate the request, and return the normalized classification payload. Extend `services/classifier/tests/test_inference_service.py` with concrete test cases for a supported invoice-like payload, an unsupported payload that resolves to `unknown_other`, and candidate-label ordering. Keep `services/classifier/tests/test_health.py` aligned with the existing health route while adding route registration assertions for the new classification endpoint.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/classifier/tests/test_health.py services/classifier/tests/test_inference_service.py -q</automated>
  </verify>
  <acceptance_criteria>`services/classifier/src/classifier_service/inference.py` contains `run_classification`; `services/classifier/src/classifier_service/main.py` contains `@app.post("/v1/classifications:run")`; `services/classifier/src/classifier_service/config.py` contains `CLASSIFIER_MODEL_VERSION` and `CLASSIFIER_CONFIDENCE_THRESHOLD`; `services/classifier/tests/test_inference_service.py` asserts both a supported taxonomy label and `unknown_other` fallback behavior.</acceptance_criteria>
  <done>The classifier service can produce deterministic Phase 4 classification outputs with explicit confidence and low-confidence policy semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add classification queue tasks and service-to-service classifier calls</name>
  <files>services/orchestrator/src/orchestrator_service/classifier_client.py, services/orchestrator/src/orchestrator_service/tasks.py, services/orchestrator/tests/test_classification_tasks.py</files>
  <read_first>services/orchestrator/src/orchestrator_service/tasks.py, services/orchestrator/src/orchestrator_service/extractor_client.py, services/orchestrator/tests/test_extraction_tasks.py, services/classifier/src/classifier_service/main.py, packages/contracts/src/doc_platform_contracts/classification.py</read_first>
  <behavior>
    - Test 1: An extracted-text completion path can enqueue or invoke a classification task using the exact classifier route `/v1/classifications:run`.
    - Test 2: Classification requests carry the extracted text, source media type, and stable job/document identifiers needed for downstream persistence.
    - Test 3: Classifier 5xx and timeout failures surface as retriable task errors instead of silent success.
  </behavior>
  <action>Create `services/orchestrator/src/orchestrator_service/classifier_client.py` with a client method `run_classification_request(...)` that posts to `/v1/classifications:run` and validates the returned `DocumentClassificationResult`. Extend `services/orchestrator/src/orchestrator_service/tasks.py` with a concrete Celery task name `document.classify.run` that accepts the extracted-text payload fields `job_id`, `document_id`, `tenant_id`, `source_media_type`, `text`, and `source_artifact_ids`, then calls the new classifier client. The classification task must preserve the extracted artifact linkage so persistence can later store both extracted-text and classification-result lineage together. Add `services/orchestrator/tests/test_classification_tasks.py` asserting task registration, request payload shape, and retry behavior on classifier client failure. Do not mark jobs completed in orchestrator task code yet; leave completion to the API-owned persistence helper in Plan 03.</action>
  <verify>
    <automated>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/orchestrator/tests/test_classification_tasks.py -q</automated>
  </verify>
  <acceptance_criteria>`services/orchestrator/src/orchestrator_service/classifier_client.py` contains `/v1/classifications:run` and `run_classification_request`; `services/orchestrator/src/orchestrator_service/tasks.py` contains `document.classify.run`; `services/orchestrator/tests/test_classification_tasks.py` asserts classifier failure retry behavior and request fields including `text` and `source_artifact_ids`.</acceptance_criteria>
  <done>The async control plane can call the classifier service with normalized extracted-text payloads and surface failures safely.</done>
</task>

</tasks>

<verification>
Run classifier and orchestrator classification tests, then grep the new route, config keys, task names, and taxonomy values to confirm the service now supports fixed-taxonomy classification.
</verification>

<success_criteria>
The classifier service can receive extracted-text requests, return deterministic fixed-taxonomy results with confidence metadata, and expose a concrete async classification entry point for orchestration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-classification-and-results/04-02-SUMMARY.md`
</output>
