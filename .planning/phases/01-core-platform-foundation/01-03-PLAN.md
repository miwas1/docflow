---
phase: 01-core-platform-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - docker-compose.yml
  - infra/terraform/README.md
  - infra/terraform/modules/platform_foundation/main.tf
  - infra/terraform/modules/platform_foundation/variables.tf
  - infra/terraform/environments/aws-dev/main.tf
  - infra/terraform/environments/gcp-dev/main.tf
  - infra/terraform/environments/aws-dev/terraform.tfvars.example
  - infra/terraform/environments/gcp-dev/terraform.tfvars.example
  - .github/workflows/foundation-ci.yml
  - docs/foundation/local-development.md
autonomous: true
requirements: [SEC-06, OPS-01]
user_setup: []
must_haves:
  truths:
    - "Developers can boot the local platform foundation with Docker Compose and the defined service dependencies."
    - "The repository contains a cloud-oriented Terraform baseline for managed AWS and GCP foundation resources without requiring Kubernetes."
    - "CI verifies the foundational service scaffolds and tests so Phase 1 remains reproducible."
  artifacts:
    - "docker-compose.yml"
    - "infra/terraform/modules/platform_foundation/main.tf"
    - "infra/terraform/environments/aws-dev/main.tf"
    - "infra/terraform/environments/gcp-dev/main.tf"
    - ".github/workflows/foundation-ci.yml"
  key_links:
    - "Docker Compose environment values map to the exact variables documented in .env.example."
    - "Terraform environment stacks call the shared platform_foundation module with cloud-specific inputs."
    - "CI invokes the same service tests and validation commands referenced by the plans."
---

<objective>
Create the local runtime and cloud deployment baseline for the platform foundation.

Purpose: Phase 1 must be runnable locally and aligned with AWS/GCP deployment from the start, without incurring Kubernetes-first complexity.
Output: Docker Compose stack, Terraform baseline for AWS and GCP managed services, CI checks, and local/cloud foundation docs.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-platform-foundation/01-CONTEXT.md
@system_design.txt
@.env.example
@README.md
@docs/foundation/storage-contract.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the local Docker Compose foundation stack</name>
  <files>docker-compose.yml, docs/foundation/local-development.md</files>
  <read_first>.planning/phases/01-core-platform-foundation/01-CONTEXT.md, system_design.txt, .env.example, README.md, docs/foundation/storage-contract.md, docker-compose.yml</read_first>
  <action>Create a root `docker-compose.yml` that starts `postgres`, `rabbitmq`, `minio`, `api`, `orchestrator`, `extractor`, and `classifier`. Use container names `doc-platform-postgres`, `doc-platform-rabbitmq`, `doc-platform-minio`, `doc-platform-api`, `doc-platform-orchestrator`, `doc-platform-extractor`, and `doc-platform-classifier`. Add healthchecks for Postgres, RabbitMQ, MinIO, and each HTTP-facing service. Wire service environment variables directly from `.env.example`, expose ports `5432`, `5672`, `15672`, `9000`, `9001`, `8000`, `8001`, and `8002`, and make the orchestrator depend on Postgres and RabbitMQ. In `docs/foundation/local-development.md`, document the exact commands `docker compose up --build`, `docker compose ps`, and the health endpoints to verify for API, extractor, and classifier. Do not add app-specific ingestion pipelines yet; the compose stack should only prove the Phase 1 runtime baseline.</action>
  <verify>docker compose config >/tmp/doc-platform-compose.out && rg -n "doc-platform-postgres|doc-platform-rabbitmq|doc-platform-minio|doc-platform-api|doc-platform-orchestrator|doc-platform-extractor|doc-platform-classifier" docker-compose.yml</verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains `doc-platform-postgres`, `doc-platform-rabbitmq`, and `doc-platform-minio`
    - `docker-compose.yml` contains `doc-platform-api`, `doc-platform-orchestrator`, `doc-platform-extractor`, and `doc-platform-classifier`
    - `docker-compose.yml` contains `healthcheck:` at least once for infrastructure services and once for HTTP services
    - `docs/foundation/local-development.md` contains `docker compose up --build`
    - `docs/foundation/local-development.md` contains `/healthz`
  </acceptance_criteria>
  <done>The local runtime baseline can be booted consistently with the expected service and infrastructure dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create Terraform baselines for AWS and GCP managed foundations</name>
  <files>infra/terraform/README.md, infra/terraform/modules/platform_foundation/main.tf, infra/terraform/modules/platform_foundation/variables.tf, infra/terraform/environments/aws-dev/main.tf, infra/terraform/environments/gcp-dev/main.tf, infra/terraform/environments/aws-dev/terraform.tfvars.example, infra/terraform/environments/gcp-dev/terraform.tfvars.example</files>
  <read_first>.planning/phases/01-core-platform-foundation/01-CONTEXT.md, .planning/PROJECT.md, system_design.txt, infra/terraform/README.md</read_first>
  <action>Create an `infra/terraform` layout with a reusable module `modules/platform_foundation`. The module should declare variables for `project_name`, `environment`, `region`, `object_storage_bucket_name`, `database_instance_name`, `message_broker_name`, and `labels`. In the AWS dev environment, wire the module for S3-compatible artifact storage, RDS PostgreSQL, and Amazon MQ RabbitMQ. In the GCP dev environment, wire the module for GCS artifact storage, Cloud SQL PostgreSQL, and a placeholder RabbitMQ strategy variable named `broker_runtime` with the concrete default value `self-managed-rabbitmq` to reflect that managed RabbitMQ may need a different path. Add example tfvars files for both clouds and a README that explains Terraform in this repo is for managed service foundations only, not Kubernetes. Keep the HCL valid and explicit; do not leave TODO-only placeholder files.</action>
  <verify>rg -n "platform_foundation|object_storage_bucket_name|database_instance_name|message_broker_name" infra/terraform && rg -n "Amazon MQ|Cloud SQL|self-managed-rabbitmq|Kubernetes" infra/terraform/README.md</verify>
  <acceptance_criteria>
    - `infra/terraform/modules/platform_foundation/variables.tf` contains `variable "project_name"`, `variable "object_storage_bucket_name"`, `variable "database_instance_name"`, and `variable "message_broker_name"`
    - `infra/terraform/environments/aws-dev/main.tf` contains `Amazon MQ` or `mq`
    - `infra/terraform/environments/gcp-dev/main.tf` contains `self-managed-rabbitmq`
    - `infra/terraform/README.md` contains `managed service foundations only`
    - `infra/terraform/README.md` contains `not Kubernetes`
  </acceptance_criteria>
  <done>The repository contains a cloud-aligned Terraform baseline for AWS and GCP foundation resources.</done>
</task>

<task type="auto">
  <name>Task 3: Add CI checks for the Phase 1 foundation</name>
  <files>.github/workflows/foundation-ci.yml</files>
  <read_first>.planning/phases/01-core-platform-foundation/01-CONTEXT.md, Makefile, services/api/tests/test_health.py, services/orchestrator/tests/test_celery_app.py, services/extractor/tests/test_health.py, services/classifier/tests/test_health.py, docker-compose.yml</read_first>
  <action>Create `.github/workflows/foundation-ci.yml` that runs on pull requests and pushes to the default branch. Use Python 3.12, install service dependencies, and run the exact commands `pytest services/api/tests/test_health.py services/orchestrator/tests/test_celery_app.py services/extractor/tests/test_health.py services/classifier/tests/test_health.py -q`, `pytest services/api/tests/test_foundation_schema.py services/api/tests/test_storage_keys.py -q`, and `docker compose config`. Add a final grep-based validation step that checks `docker-compose.yml` contains the seven expected service names so CI fails if the local foundation stack drifts. Keep the workflow focused on Phase 1 foundation verification, not full product tests.</action>
  <verify>rg -n "pytest services/api/tests/test_health.py services/orchestrator/tests/test_celery_app.py services/extractor/tests/test_health.py services/classifier/tests/test_health.py -q|pytest services/api/tests/test_foundation_schema.py services/api/tests/test_storage_keys.py -q|docker compose config" .github/workflows/foundation-ci.yml</verify>
  <acceptance_criteria>
    - `.github/workflows/foundation-ci.yml` contains `python-version: "3.12"`
    - `.github/workflows/foundation-ci.yml` contains `docker compose config`
    - `.github/workflows/foundation-ci.yml` contains `services/api/tests/test_health.py`
    - `.github/workflows/foundation-ci.yml` contains `services/api/tests/test_foundation_schema.py`
    - `.github/workflows/foundation-ci.yml` contains `doc-platform-orchestrator`
  </acceptance_criteria>
  <done>Foundation CI exists and checks the key runnable/runtime artifacts introduced by Phase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docker compose config`
- [ ] `rg -n "doc-platform-postgres|doc-platform-rabbitmq|doc-platform-minio|doc-platform-api|doc-platform-orchestrator|doc-platform-extractor|doc-platform-classifier" docker-compose.yml`
- [ ] `rg -n "platform_foundation|self-managed-rabbitmq|managed service foundations only|not Kubernetes" infra/terraform`
- [ ] `rg -n "python-version: \"3.12\"|docker compose config|services/api/tests/test_foundation_schema.py" .github/workflows/foundation-ci.yml`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The platform foundation is locally runnable and cloud-aligned for AWS/GCP without requiring Kubernetes first
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-platform-foundation/01-03-SUMMARY.md`
</output>
