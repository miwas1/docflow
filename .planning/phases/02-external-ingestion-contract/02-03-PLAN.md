---
phase: 02-external-ingestion-contract
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - services/api/src/api_service/repositories/jobs.py
  - services/api/src/api_service/schemas.py
  - services/api/src/api_service/services/status.py
  - services/api/src/api_service/main.py
  - services/api/tests/test_status_api.py
  - docs/foundation/status-polling-contract.md
  - README.md
autonomous: true
requirements: [DLV-01, DLV-02]
user_setup: []
must_haves:
  truths:
    - "Clients can poll a status endpoint for queued, running, completed, and failed jobs."
    - "Status responses expose stage-based lifecycle information, stable identifiers, timestamps, and terminal failure details."
    - "The polling contract is documented consistently with the API implementation."
  artifacts:
    - "services/api/src/api_service/services/status.py"
    - "services/api/src/api_service/main.py"
    - "services/api/tests/test_status_api.py"
    - "docs/foundation/status-polling-contract.md"
  key_links:
    - "Status responses derive from the same `jobs` and `job_events` persistence contract populated by Phase 2 upload acceptance."
    - "The public polling contract must match the stage-based semantics locked in CONTEXT.md."
---

<objective>
Deliver the stage-based polling contract for external clients.

Purpose: After upload acceptance exists, external clients need a consistent way to observe lifecycle state, current stage, and failure details without waiting for later webhook work.
Output: Status service/repository logic, polling endpoint, tests, and polling contract documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-external-ingestion-contract/02-CONTEXT.md
@.planning/phases/02-external-ingestion-contract/02-02-PLAN.md
@services/api/src/api_service/main.py
@services/api/src/api_service/db/models.py
@services/api/src/api_service/repositories/jobs.py
@services/api/src/api_service/schemas.py
@docs/foundation/external-ingestion-api.md
@system_design.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement stage-based polling responses</name>
  <files>services/api/src/api_service/repositories/jobs.py, services/api/src/api_service/schemas.py, services/api/src/api_service/services/status.py, services/api/src/api_service/main.py, services/api/tests/test_status_api.py</files>
  <read_first>.planning/phases/02-external-ingestion-contract/02-CONTEXT.md, .planning/REQUIREMENTS.md, services/api/src/api_service/main.py, services/api/src/api_service/db/models.py, services/api/src/api_service/repositories/jobs.py, services/api/src/api_service/schemas.py, services/api/tests/test_upload_api.py</read_first>
  <action>Add a polling surface to the API service. Create `services/api/src/api_service/services/status.py` that loads a job by `job_id` scoped to the authenticated client and returns a stage-based view with exact fields `job_id`, `document_id`, `status`, `current_stage`, `created_at`, `updated_at`, `accepted_at`, and `failure` where `failure` is either `null` or an object containing `code` and `message`. Update `services/api/src/api_service/schemas.py` so the status response model includes those exact fields and does not include percentage progress. Extend `services/api/src/api_service/repositories/jobs.py` with concrete query helpers for fetching one job plus its latest relevant failure details. Update `services/api/src/api_service/main.py` to expose `GET /v1/jobs/{job_id}` protected by the same static API-key auth used for uploads, returning `404` when the job is unknown to that client and the documented status shape otherwise. Add `services/api/tests/test_status_api.py` covering queued/running/completed/failed lifecycle examples, authenticated access, `404` for unknown jobs, and the presence of `failure.code` / `failure.message` only for failed jobs.</action>
  <verify>PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_status_api.py -q</verify>
  <acceptance_criteria>
    - `services/api/src/api_service/main.py` contains `@app.get("/v1/jobs/{job_id}")`
    - `services/api/src/api_service/schemas.py` contains `accepted_at`
    - `services/api/src/api_service/schemas.py` contains `failure`
    - `services/api/src/api_service/services/status.py` contains `current_stage`
    - `services/api/tests/test_status_api.py` asserts `404` for unknown jobs
    - `services/api/tests/test_status_api.py` asserts `failure` is present for failed jobs
  </acceptance_criteria>
  <done>The API exposes a stable stage-based polling contract for external clients.</done>
</task>

<task type="auto">
  <name>Task 2: Document the status polling contract</name>
  <files>docs/foundation/status-polling-contract.md, README.md</files>
  <read_first>.planning/phases/02-external-ingestion-contract/02-CONTEXT.md, docs/foundation/external-ingestion-api.md, services/api/src/api_service/schemas.py, README.md</read_first>
  <action>Create `docs/foundation/status-polling-contract.md` documenting the exact route `GET /v1/jobs/{job_id}`, the stage-based response fields `job_id`, `document_id`, `status`, `current_stage`, `created_at`, `updated_at`, `accepted_at`, and optional `failure.code` / `failure.message`, plus example payloads for queued and failed jobs. Update `README.md` with a short Phase 2 section that links both `docs/foundation/external-ingestion-api.md` and `docs/foundation/status-polling-contract.md` as the external client contract docs.</action>
  <verify>rg -n "GET /v1/jobs/\\{job_id\\}|current_stage|accepted_at|failure.code|failure.message" docs/foundation/status-polling-contract.md && rg -n "external-ingestion-api|status-polling-contract" README.md</verify>
  <acceptance_criteria>
    - `docs/foundation/status-polling-contract.md` contains `GET /v1/jobs/{job_id}`
    - `docs/foundation/status-polling-contract.md` contains `current_stage`
    - `docs/foundation/status-polling-contract.md` contains `accepted_at`
    - `docs/foundation/status-polling-contract.md` contains `failure.code`
    - `README.md` contains `external-ingestion-api`
    - `README.md` contains `status-polling-contract`
  </acceptance_criteria>
  <done>The public polling contract is documented alongside the upload API docs for integrators.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest services/api/tests/test_status_api.py -q`
- [ ] `rg -n "@app.get\\(\"/v1/jobs/\\{job_id\\}\"\\)|current_stage|accepted_at|failure" services/api/src/api_service/main.py services/api/src/api_service/services/status.py services/api/src/api_service/schemas.py`
- [ ] `rg -n "GET /v1/jobs/\\{job_id\\}|failure.code|failure.message|status-polling-contract" docs/foundation/status-polling-contract.md README.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- External clients can poll lifecycle state and failure details through a documented stage-based status contract
</success_criteria>

<output>
After completion, create `.planning/phases/02-external-ingestion-contract/02-03-SUMMARY.md`
</output>
